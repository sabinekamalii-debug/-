# 干员玩法设计

> 参考明日方舟肉鸽（集成战略）模式，结合本项目已有架构（OperatorData / DeploymentManager / RogueRuntimeState / 天赋卡 / 金币商店 / StS 分叉地图），设计一套完整的干员持有、获取、升级、升星系统。
>
> **实现状态**（2026-08-12）：Phase 1~2 + Phase 4（强化&UI）已落地；Phase 3（局内获取）和 Phase 5（跨大局继承）尚未实装。

---

## 一、设计概述

### 1.1 两套系统各司其职

| 系统 | 定位 | 对标 | 获取方式 |
|------|------|------|---------|
| 天赋卡 | 遗物（被动加成） | 杀塔2 遗物 / 以撒 道具 | 战后 3 选 1（现有系统不变）+ 商店购买 |
| 干员 | 角色阵容（战斗单位） | 明日方舟 干员 | 开局自选 + 剧情获取（主）+ 奇遇/商店概率（辅） |

### 1.2 设计目标

1. **开局自选**：玩家在 RogueEntry 场景从已解锁的干员中自由搭配初始队伍
2. **星级 1~5**：干员分 1~5 星，初始都是 1 星状态，但按最高星级上限不同，1 星的实力也有差别
3. **星数预算 7**：选中干员的当前星数总和不超过 7，约束队伍强度
4. **星级强化**：消耗金币提升干员星级（1→2→...→上限），5 星干员最多接受 4 次强化
5. **剧情获取（主）**：约一半干员通过剧情事件获取
6. **概率获取（辅）**：奇遇/商店概率出现，偶尔有策划安排的剧情事件
7. **单实例部署**：每名干员同时只能 1 个在场，撤退/阵亡后需等再部署冷却
8. **跨大局继承**：通关大局后选 5 名干员带入下一个大局，其余因剧情离队

### 1.3 核心设计原则

- **不破坏现有战斗循环**：OperatorCard 拖拽部署、DP 费用系统、OperatorUnit 战斗逻辑保持不变
- **不引入新货币**：只用现有金币（RunGold）
- **天赋卡 = 遗物**：战后 3 选 1 只出天赋卡，不混入干员
- **干员 = 角色**：开局自选 + 剧情获取，有叙事重量
- **策划主导**：干员获取点由策划手动配置在 ActConfig 中

---

## 二、干员星级体系

### 2.1 星级定义（1~5 星）

| 星级 | 对标明日方舟 | 定位 | 强化次数 | 数量 |
|------|-------------|------|---------|------|
| ★ | 1~2★ | 炮灰/工具人 | 最多 1 次强化到 ★★ | ~6 |
| ★★ | 3★ | 低费铺场/过渡用 | 最多 1 次强化到 ★★★ | ~10 |
| ★★★ | 4★ | 中坚战力 | 最多 1 次强化到 ★★★★ | ~7 |
| ★★★★ | 5★ | 核心战力 | 最多 1 次强化到 ★★★★★ | ~4 |
| ★★★★★ | 6★ | 决战单位/隐藏角色 | 已满星，不可强化 | ~2 |

> **关键设计**：所有干员初始都是 1 星（当前星级 = 1），但 `maxStarRating`（最高星级上限）不同。一个 maxStarRating=5 的干员在 1 星状态比 maxStarRating=2 的干员在 1 星状态更强——星级上限代表了干员的潜力，1 星只是起点。

### 2.2 1 星状态下的实力差异

干员在 1 星时的基础属性由 `maxStarRating` 决定倍率：

```
1 星基础属性 = OperatorData 原始值 × baseStatMultiplier(maxStarRating)

maxStarRating=1: ×0.6   (炮灰级，极弱)
maxStarRating=2: ×0.8   (过渡级，偏低)
maxStarRating=3: ×1.0   (标准级，正常)
maxStarRating=4: ×1.3   (精英级，偏强)
maxStarRating=5: ×1.6   (核心级，很强)
```

> 即使都是 1 星，一个 5 星上限的干员（×1.6）比一个 1 星上限的干员（×0.6）强近 3 倍。这让高星上限的干员即使没强化也值得带上。

### 2.3 星级强化

消耗金币提升干员当前星级（currentStar → currentStar+1），每提升 1 星全属性大幅增长：

```
强化后属性倍率 = baseStatMultiplier(maxStarRating) × starGrowth(currentStar)

starGrowth: ★=1.0, ★★=1.3, ★★★=1.7, ★★★★=2.2, ★★★★★=3.0
```

| 当前星级 | → 下一星级 | 属性倍率变化 | 强化费用 |
|---------|-----------|------------|---------|
| ★1 | → ★2 | ×1.0 → ×1.3 | 20 金币 |
| ★2 | → ★3 | ×1.3 → ×1.7 | 50 金币 |
| ★3 | → ★4 | ×1.7 → ×2.2 | 100 金币 |
| ★4 | → ★5 | ×2.2 → ×3.0 | 200 金币 |

> 强化受 `maxStarRating` 限制：maxStarRating=2 的干员最多强化到 ★2，maxStarRating=5 的干员最多强化到 ★5（4 次强化）。

### 2.4 现有 42 名干员星级分配

> 以下数据与 OperatorData.asset 实际 maxStarRating 字段一致。

#### maxStarRating=1（1 星上限，7 名）— 炮灰/工具人

| 干员 | cost | 理由 |
|------|------|------|
| 地面射手 | 20 | 低血低属性，炮灰级 |
| 拳师 | 14 | 低血近战，工具人 |
| 光波 | 26 | AoE 但 HP=1，极脆 |
| 先锋测试 | 6 | 测试用基础单位 |
| 突击先锋 | 3 | 最低费，数值极低 |
| 游击先锋 | 5 | 低费低属性 |
| 醒 | 0 | 超高属性但 600s 冷却，特殊剧情获取 |

#### maxStarRating=2（2 星上限，8 名）— 低费铺场型

| 干员 | cost | 理由 |
|------|------|------|
| 斥候先锋 | 4 | 低费侦察标记 |
| 风暴先锋 | 7 | 低费速攻 |
| 猎手先锋 | 6 | 中低费先锋 |
| 驻防先锋 | 8 | 中低费防御型 |
| 坚守先锋 | 9 | 中低费重装 |
| 连弩 | 14 | 狙击入门 |
| 净化 | 20 | 医疗入门 |
| 晶 | 15 | 中坚近战 |

#### maxStarRating=3（3 星上限，12 名）— 中坚战力

| 干员 | cost | 理由 |
|------|------|------|
| 战术先锋 | 7 | 均衡先锋 |
| 武士 | 16 | 中防近战 |
| 牧师 | 16 | 医疗中坚 |
| 圣光 | 18 | 高防医疗 |
| 铁壁 | 18 | 重装入门 |
| 鹰眼 | 16 | 狙击中坚 |
| 荆棘 | 22 | 重装反伤 |
| 巨蟹 | 18 | 重装坦克 |
| 星豹 | 6 | 先锋 |
| 死亡剑士 | 5 | 先锋 |
| 火焰战士 | 11 | 近卫 |
| 藤曼战士 | 16 | 重装 |

#### maxStarRating=4（4 星上限，6 名）— 核心战力

| 干员 | cost | 理由 |
|------|------|------|
| 珑 | 20 | 高防高血近战 |
| 奥术 | 35 | 术师高伤 |
| 法师 | 45 | 最强术师 |
| 傀儡师 | 12 | 近卫 |
| 死囚 | 20 | 重装扛伤+控制 |
| 紫雷狼 | 7 | 先锋 |

#### maxStarRating=5（5 星上限，5 名）— 决战单位

| 干员 | cost | 理由 |
|------|------|------|
| 近卫 | 9 | 超高血(1200)+高攻(130)，近卫核心 |
| 万录朵 | 50 | Boss 级干员 |
| 时之魔王 | 20 | 特种，暂停全图敌人 |
| 狂兽战士 | 14 | 近卫，龙血沸腾 |
| 空之魔王 | 22 | 狙击，地面+高台远程射手 |
| 雪女 | 40 | 术师，冰封千里 |

> **注意**：`maxStarRating` 标注在 OperatorData 上，不改动现有数值。`currentStar` 是运行时状态，初始值=1。女战士(21, maxStar=2)和钩爪(15, maxStar=2)为旧干员，数值设计汇总中可见完整列表。

---

## 三、开局干员选择系统

### 3.1 RogueEntry 场景布局

利用 RogueEntry 场景中间的大片空区域，新增干员选择 UI：

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│  天赋点:42    永久点数:42                                │
│                    ┌───────────┐                         │
│                    │           │                  ┌──┐   │
│                    │  干员立绘  │            ┌──┐  │头│   │
│                    │  (大图)   │            │头│  │像│   │
│                    │           │            │像│  │  │   │
│  [开始本局]        │           │            │  │  │  │   │
│                    └───────────┘            └──┘  └──┘   │
│  "和我同行吗？     战术先锋 ★→★★★            ┌──┐ ┌──┐  │
│   可别拖后腿。"    当前:★1  上限:★★★        │头│ │头│  │
│                                          │像│ │像│  │
│  [天赋树] [收藏页]    星数:3/7          └──┘ └──┘  │
│  [GameMode▼]        已选:斥候/战术/连弩                  │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**三区域布局**：

| 区域 | 位置 | 内容 | 交互 |
|------|------|------|------|
| 左侧 | 现有按钮区域 | 开始本局/天赋树/收藏/GameMode + 星数预算显示 | 现有功能不变 |
| 中间 | 场景空区域 | 大尺寸干员立绘 + 台词 + 名称/星级/属性 | 悬停预览 |
| 右侧 | 场景右侧 | 干员头像列表（网格排列），分"已解锁"和"未解锁" | 点击预览→再点选中/取消 |

### 3.2 干员头像列表（右侧）

```
┌──────────────────┐
│ 已解锁 (12)      │
├──────────────────┤
│ ┌──┐ ┌──┐ ┌──┐  │
│ │头│ │头│ │头│  │  ← 已解锁干员头像
│ └──┘ └──┘ └──┘  │
│ ┌──┐ ┌──┐ ┌──┐  │
│ │头│ │头│ │头│  │  ← 选中状态：边框高亮 + 角标数字
│ └──┘ └──┘ └──┘  │
├──────────────────┤
│ 未解锁 (17)      │
├──────────────────┤
│ ┌──┐ ┌──┐ ┌──┐  │
│ │？│ │？│ │？│  │  ← 未解锁干员：灰色剪影 + 问号
│ └──┘ └──┘ └──┘  │
└──────────────────┘
```

- 已解锁干员：正常头像，按 maxStarRating 排序（高星在前）
- 选中状态：边框金色高亮 + 右上角角标显示选中顺序（1/2/3...）
- 未解锁干员：灰色剪影 + 问号，点击显示"尚未解锁，通过剧情获取"
- 头像列表可滚动

### 3.3 中间预览区

点击右侧头像后，中间区域显示该干员：

```
┌───────────────────────┐
│                       │
│     ┌───────────┐     │
│     │           │     │
│     │  干员立绘  │     │  ← 大尺寸立绘图
│     │  (全身)   │     │
│     │           │     │
│     └───────────┘     │
│                       │
│  "和我同行吗？         │  ← 干员台词（策划配置）
│   可别拖后腿。"        │
│                       │
│  战术先锋  ★→★★★      │  ← 名称 + 当前星级→上限
│  当前:★1  预算占用:1   │  ← 当前星数 + 占用预算
│  HP:130  ATK:21        │  ← 1 星状态下的属性
│  职业:先锋  cost:7     │  ← 职业类型 + DP 费用
│                       │
│  [选中] / [取消选中]   │  ← 再次点击 = 选中/取消
└───────────────────────┘
```

### 3.4 星数预算系统

| 参数 | 值 | 说明 |
|------|-----|------|
| 星数预算上限 | 7 | 所有选中干员的 currentStar 之和不超过 7 |
| 最少选择数量 | 3 | 至少选 3 名干员才能出发（支持1核心+2辅助的小阵容） |
| 最多选择数量 | 8 | 受星数预算约束，理论上最多 7 名 1 星干员 |

**示例组合**：

| 组合 | 星数 | 说明 |
|------|------|------|
| 1 名 ★5 + 1 名 ★2 | 5+2=7 | 极限单核 + 辅助 |
| 1 名 ★3 + 2 名 ★2 | 3+2+2=7 | 均衡阵容 |
| 3 名 ★2 + 1 名 ★1 | 2+2+2+1=7 | 多人铺场 |
| 7 名 ★1 | 1×7=7 | 人海战术 |

> 开局时所有干员都是 1 星（currentStar=1），所以初始预算 = 选中数量 × 1。随着游戏中花费金币强化星级，星数会增长，但开局预算 7 限制了初始队伍规模。

### 3.5 干员解锁状态

干员解锁是**永久的**（跨大局保留）：

| 状态 | 说明 |
|------|------|
| 已解锁 | 玩家可通过该干员的历史剧情/首通解锁，在开局选择时可选 |
| 未解锁 | 灰色显示，需要通过特定大局的剧情事件解锁 |

> 解锁状态存储在 PlayerPrefs（与天赋树一样是永久的）。首次通过剧情获取某干员后自动解锁，之后每次开局都可以选择。

### 3.6 干员台词

每个干员配置一句选择台词（策划在 OperatorData 中配置）：

```csharp
// OperatorData 新增
[Tooltip("干员选择台词（RogueEntry 界面展示）")]
public string selectQuote = "";
```

**示例**：
| 干员 | 台词 |
|------|------|
| 战术先锋 | "和我同行吗？可别拖后腿。" |
| 斥候先锋 | "侦察就交给我吧，保证不会漏掉任何敌人。" |
| 连弩 | "弓弦已上好，只等你一声令下。" |
| 净化 | "伤员交给我，你们只管往前冲。" |
| 近卫 | "吾之剑，为正义而挥。" |
| 鹰眼 | "远处的东西，在我眼中和近处没什么两样。" |
| 万录朵 | "...你看起来很有趣。就陪你走一程吧。" |

---

## 四、局内干员持有系统

### 4.1 干员名册（OperatorRoster）

```
RogueOperatorState
├── operatorDataId    (string)  — 对应 OperatorData 的名字/ID
├── maxStarRating     (int)     — 星级上限（1~5，不变）
├── currentStar       (int)     — 当前星级（1~maxStarRating，可强化提升）
├── isInitial         (bool)    — 是否开局选中的干员
├── acquisitionMethod (enum)    — 获取方式（Selection / Story / Encounter / Shop）
├── sourceActId       (int)     — 在哪个大局获得的
└── unlocked          (bool)    — 是否已永久解锁（跨大局保留）
```

### 4.2 局内阵容

| 参数 | 值 | 说明 |
|------|-----|------|
| 开局星数预算 | 7 | 选中干员 currentStar 之和 |
| 最少选择人数 | 3 | 至少选 3 名干员才能开战 |
| 最多选择人数 | 8 | RogueEntry 最多可选 8 名干员进局 |
| 跨大局携带上限 | 5 | 通关大局后选 5 名带入下一个大局（尚未实装） |

### 4.3 战斗中的干员卡牌（已实现 ✅）

- 战斗场景加载后，`RosterDeployInitializer`（全局自动生效，`[RuntimeInitializeOnLoadMethod]`）读取 `RogueRuntimeState.SelectedRoster`，把场景里静态摆放的 OperatorCard 按选中的阵容重新绑定
- 阵容人数多于场景已有卡片数时自动克隆补齐；多余的原始卡片自动隐藏
- `OperatorCard.ApplyRosterOperator(data)` 绑定数据 + `RefreshStarBadge()` 显示当前星级
- 升星面板（`OperatorStarUpgradePanel`）在战斗场景和商店场景自动注入，无需手动摆放 UI

---

## 五、干员获取系统（尚未实装 ❌）

> 以下获取路径均未落地。当前干员仅通过开局自选获得，局内无法获取新干员。

### 5.1 获取方式总览

| 方式 | 时机 | 费用 | 说明 |
|------|------|------|------|
| **开局自选** | RogueEntry 界面 | 免费 | 从已解锁干员中选初始队伍 |
| **剧情获取** | 特定关卡/节点 | 免费/金币 | 策划在 ActConfig 手动配置 |
| **奇遇获取** | 奇遇事件节点 | 免费/金币 | 部分奇遇选项奖励干员 |
| **商店概率** | 商店节点 | 金币 | 60% 概率刷新出可招募干员 |
| **商店剧情** | 商店/休息节点 | 金币/免费 | 策划安排的剧情事件 |

> 新获取的干员默认 currentStar=1，需要花费金币强化星级。

### 5.2 剧情获取（主路径）

策划在 ActConfig 中配置"剧情干员获取点"：

```csharp
[System.Serializable]
public class StoryOperatorAcquisition
{
    public int nodeIndex;
    public string operatorName;
    public string storyTriggerId;
    public bool isFree;
    public int cost;
    public string joinReason;
}
```

**示例**（大局 1 · 魔王的试炼）：

| 节点 | 干员 | maxStar | 剧情原因 |
|------|------|---------|---------|
| 节点 3 | 武士 | ★★★ | "武者流浪至此，愿同行" |
| 节点 7 | 牧师 | ★★★ | "神殿牧师感念你的使命，决定随行" |
| 节点 12 | 铁壁 | ★★★ | "佣兵要求预付佣金才肯加入"（30 金币） |
| 节点 18 | 奥术 | ★★★★ | "被囚禁的术师获救后自愿加入" |

> 剧情获取的干员同时**永久解锁**，之后开局自选时也可以选。

### 5.3 奇遇获取

部分奇遇事件选项奖励干员（从未持有的同星级上限干员池随机抽取）。

### 5.4 商店概率刷新

| 参数 | 值 |
|------|-----|
| 出现概率 | 60% |
| 货位数量 | 1~2 |
| 招募价格 | 按 maxStarRating：1星=10金 / 2星=20金 / 3星=40金 / 4星=80金 / 5星=150金 |

### 5.5 放弃干员

- 剧情/奇遇获取的干员放弃时不返还金币
- 商店购买的干员放弃时返还 50% 金币
- 开局选中的干员不可放弃

---

## 六、星级强化系统（已实现 ✅）

### 6.1 强化机制

消耗金币提升干员 currentStar：

| currentStar → 下一星 | 费用 | 属性倍率变化 |
|---------------------|------|------------|
| ★1 → ★2 | 20 金币 | ×1.0 → ×1.3 |
| ★2 → ★3 | 50 金币 | ×1.3 → ×1.7 |
| ★3 → ★4 | 100 金币 | ×1.7 → ×2.2 |
| ★4 → ★5 | 200 金币 | ×2.2 → ×3.0 |

> 强化受 maxStarRating 限制。强化在商店/休息节点可用。

### 6.2 属性计算

```
最终属性 = OperatorData 域始值 × baseStatMultiplier(maxStarRating) × starGrowth(currentStar)

baseStatMultiplier: maxStar=1→0.6, maxStar=2→0.8, maxStar=3→1.0, maxStar=4→1.3, maxStar=5→1.6
starGrowth: ★1→1.0, ★2→1.3, ★3→1.7, ★4→2.2, ★5→3.0
```

**示例**（战术先锋 maxStar=3, ATK=21）：
- ★1（初始）: 21 × 1.0 × 1.0 = 21
- ★2（强化1次）: 21 × 1.0 × 1.3 = 27
- ★3（强化2次）: 21 × 1.0 × 1.7 = 36

**示例**（近卫 maxStar=5, ATK=130）：
- ★1（初始）: 130 × 1.6 × 1.0 = 208
- ★2（强化1次）: 130 × 1.6 × 1.3 = 270
- ★5（强化4次）: 130 × 1.6 × 3.0 = 624

### 6.3 强化与天赋卡的叠加

```
最终属性 = (OperatorData 原始值 × baseStatMultiplier × starGrowth)  ← 星级系统
         × (1 + 天赋卡百分比加成)                                    ← 天赋卡
         × (1 + 天赋树百分比加成)                                    ← 天赋树
```

### 6.4 满星被动（已实现 ✅）

干员达到 maxStarRating（满星）时解锁职业被动（`OperatorUnit.ApplyStarPassive()`）：

| 职业 | 满星被动 | 效果（与代码一致） |
|------|---------|------|
| 先锋 | 部署返还 | 部署即返还部署费用 + 技力回复加速 30% |
| 近卫 | 专注强化 | 攻击力 +30% |
| 重装 | 钢铁意志 | 防御 +40% + 致命伤保留 1 HP（每场限 1 次） |
| 狙击 | 致命瞄准 | 暴击率 +25% |
| 术师 | 法术穿透 | 无视 50% 防御 + 攻击力 +20% |
| 医疗 | 紧急救护 | 治疗量 +30% |
| 特种 | 战术机动 | 撤退返还 +50%（再部署更划算） |

---

## 七、跨大局继承系统（尚未实装 ❌）

### 7.1 核心机制

通关大局后，选择 5 名干员带入下一个大局：

- 选中的 5 名：保留 currentStar 和解锁状态
- 未选中的：因剧情离队（显示离队文案），但**解锁状态永久保留**
- 天赋卡/金币：全部清空

### 7.2 离队剧情

```csharp
[System.Serializable]
public class OperatorDepartureReason
{
    public string operatorName;
    public string departureReason;
}
```

**示例**：
- 武士："武者需回乡探望年迈的母亲，在此别过。"
- 鹰眼："我的箭矢需要指向另一个方向了。"

### 7.3 跨大局数据保留

| 保留 | 清空 |
|------|------|
| 选中的 5 名干员（currentStar 保留） | 未选中的干员（本局不再可用） |
| 所有已解锁干员的解锁状态（永久） | 天赋卡 |
| 天赋树（永久） | 金币 |

---

## 八、数据结构变更（已实现 ✅）

### 8.1 OperatorData 新增字段（已实现 ✅）

```csharp
[Header("星级")]
[Tooltip("星级上限：1~5。所有干员初始为1星，可强化到此上限")]
public int maxStarRating = 1;

[Tooltip("满星(=maxStarRating)解锁的职业被动说明文案")]
[TextArea(2, 3)]
public string starPassiveDesc = "";

[Header("台词")]
[Tooltip("干员选择台词（RogueEntry 界面展示）")]
[TextArea(2, 3)]
public string selectQuote = "";

[Tooltip("是否为初始可用干员（开局即可选）")]
public bool isInitialAvailable = false;

/// 养成名册唯一 key（用 operatorName）
public string RegistryKey => string.IsNullOrEmpty(operatorName) ? name : operatorName;
```

### 8.2 RogueRuntimeState 新增（已实现 ✅）

```csharp
// 选人阵容（开局写入，跨场景持久化）
private static List<string> _selectedRoster;
public static IReadOnlyList<string> SelectedRoster => _selectedRoster;

public static void SetSelectedRoster(List<string> keys, IEnumerable<OperatorData> allData);
public static void ClearSelectedRoster();
public static bool TryUpgradeOperatorStar(string key, IEnumerable<OperatorData> allData, out int cost);
```

> 注：设计文档原规划的 `RogueOperatorState` 类/`OperatorRoster` 列表/`CarriedOperators`/`UnlockedOperators` 未采用；
> 实际实现简化为 `SelectedRoster`（string 列表）+ `OperatorStarRegistry`（星级字典），更轻量。

### 8.3 OperatorStarRegistry（实际新增文件，替代原设计的 RogueOperatorState）

```csharp
public static class OperatorStarRegistry
{
    public static bool IsRunActive { get; }
    public static event Action<string> OnStarChanged;

    public static void BeginRun(IEnumerable<string> selectedKeys, IEnumerable<OperatorData> allData);
    public static void EndRun();
    public static int GetStar(string key);
    public static int GetMaxStar(string key, IEnumerable<OperatorData> allData);
    public static bool IsMaxStar(string key, IEnumerable<OperatorData> allData);
    public static bool TryUpgradeStar(string key, IEnumerable<OperatorData> allData, out int cost);
    public static int PreviewUpgradeCost(string key, IEnumerable<OperatorData> allData);
    public static List<string> GetRosterKeys();
    // PlayerPrefs 持久化（跨场景保留星级）
}
```

### 8.4 BalanceConfig 新增（已实现 ✅）

```csharp
public static readonly int[] StarUpgradeCost = { 20, 50, 100, 200 };   // 升到★2/★3/★4/★5
public const int MaxStarLimit = 5;
public const int RosterMinCount = 3;      // 最少选3人
public const int RosterMaxCount = 8;      // 最多选8人
public const int StarBudget = 7;          // 星数预算

public static readonly float[] BaseStatMultiplier = { 0f, 0.6f, 0.8f, 1.0f, 1.3f, 1.6f };
public static readonly float[] StarGrowth = { 0f, 1.0f, 1.3f, 1.7f, 2.2f, 3.0f };

public static int GetStarUpgradeCost(int targetStar);
```

> 注：原设计的 `CrossActCarryLimit` / `RecruitCostByMaxStar` / `ShopOperatorAppearanceChance` /
> `RetreatDpRefundRatio` / `DeathCooldownMultiplier` / `AbandonRefundRatio` 尚未加入（对应功能未实装）。

### 8.5 ActConfig 新增（尚未实装 ❌）

原设计的 `storyOperatorAcquisitions` / `departureReasons` / `shopOperatorEvents` 字段尚未添加。

### 8.6 属性计算（已实现 ✅）

```csharp
// OperatorUnit.StarStatMultiplier()
float StarStatMultiplier()
{
    int idxMax = Clamp(data.maxStarRating, 1, BaseStatMultiplier.Length - 1);
    int idxStar = Clamp(CurrentStar, 1, StarGrowth.Length - 1);
    return BaseStatMultiplier[idxMax] * StarGrowth[idxStar];
}
// runtimeMaxHealth / runtimeAttackDamage / runtimeDefense = 原始值 × StarStatMultiplier()
```

---

## 九、与现有系统的集成

### 9.1 天赋卡 vs 干员：完全分离

| 维度 | 天赋卡 | 干员 |
|------|--------|------|
| 获取渠道 | 战后 3 选 1 + 商店 | 开局自选 + 剧情 + 奇遇 + 商店概率 |
| 作用对象 | 全队/职业/专属 | 单个干员 |
| 跨大局保留 | 不保留 | 保留 5 名（玩家选择）+ 解锁状态永久 |
| 叠加方式 | TalentEffectApplier | OperatorUnit.Start() 计算 |

### 9.2 单实例部署机制（部分实现 ⚠️）

| 规则 | 说明 | 状态 |
|------|------|------|
| 单实例 | 同一干员在场时卡牌灰化"部署中" | ⚠️ 待确认 |
| 撤退 | 返还 50% DP，再部署冷却 = purchaseCooldown | ⚠️ 待确认 |
| 阵亡 | 不返还 DP，再部署冷却 = purchaseCooldown × 1.5 | ❌ 冷却×1.5 未实装 |

### 9.3 与 RogueEntry 场景的集成（已实现 ✅）

RogueEntry 场景的 OperatorSelectionPanel 已接入：
- `RogueEntryController.StartRun()` 先调用 `panel.CanStart()` 检查阵容合法性，再调用 `panel.ConfirmRoster()` 写入 `RogueRuntimeState.SetSelectedRoster()`
- 阵容不合法（人数/星数预算不满足）时不进入战斗，等待玩家调整

### 9.4 与商店系统集成（部分实现 ⚠️）

- 商店已自动注入 `OperatorStarUpgradePanel`（升星面板），可在商店花金币升星 ✅
- 商店招募干员分页（60% 概率出现可招募干员）❌ 尚未实装

### 9.5 与 ActConfig 集成（尚未实装 ❌）

ActConfig 尚未添加干员配置字段：
```
ActConfig（规划中）
├── initialOperators[]           — 初始可用干员（未添加）
├── storyOperatorAcquisitions[]  — 剧情获取点（未添加）
├── shopOperatorEvents[]         — 商店剧情事件（未添加）
├── departureReasons[]           — 离队文案（未添加）
└── bossNodeIndex / totalNodes   — 现有字段
```

---

## 十、UI 设计

### 10.1 RogueEntry 干员选择界面

```
┌──────────────────────────────────────────────────────────┐
│  天赋点:42          星数预算: 3/7                        │
│                                                          │
│                    ┌───────────┐     ┌──┐┌──┐┌──┐      │
│                    │           │     │头││头││头│      │
│                    │  立绘     │     └──┘└──┘└──┘      │
│                    │           │     ┌──┐┌──┐┌──┐      │
│  [开始本局]         │           │     │头││头││头│      │
│                    └───────────┘     └──┘└──┘└──┘      │
│  "和我同行吗？      战术先锋          ┌──┐┌──┐┌──┐      │
│   可别拖后腿。"     ★→★★★           │头││头││？│      │
│                    当前:★1           └──┘└──┘└──┘      │
│  [天赋树][收藏]     HP:130 ATK:21     已解锁    未解锁    │
│  [GameMode▼]                                         │
│                    [选中/取消]                         │
└──────────────────────────────────────────────────────────┘
```

### 10.2 战斗中干员卡牌

```
┌─────────────┐
│ ★(→★★★) Lv.1│  ← 当前星(→上限) + 等级
│  ┌───────┐  │
│  │ 立绘  │  │
│  └───────┘  │
│  战术先锋   │
│  cost: 7   │
└─────────────┘
```

### 10.3 跨大局整备界面

```
┌──────────────────────────────────────────┐
│  大局 1 通关！整备阶段                    │
│  选择 5 名干员带入下一个大局              │
│  已选: 3/5                                │
├──────────────────────────────────────────┤
│  ✓ [战术先锋 ★2]   ← 已选（保留★2）     │
│  ✓ [连弩 ★1]       ← 已选                │
│  ✓ [近卫 ★3]       ← 已选（满星★3）      │
│  ☐ [武士 ★1]       ← 未选                │
│  ☐ [鹰眼 ★2]       ← 未选                │
├──────────────────────────────────────────┤
│  离队预览:                               │
│  武士: "武者需回乡探望年迈的母亲，在此别过。"│
│  鹰眼: "我的箭矢需要指向另一个方向了。"    │
├──────────────────────────────────────────┤
│  [确认出发] (选满5名后可用)               │
└──────────────────────────────────────────┘
```

---

## 十一、实际文件清单

### 已创建的新文件 ✅

| 文件路径 | 类型 | 说明 |
|---------|------|------|
| `Assets/塔防脚本/干员/OperatorStarRegistry.cs` | 静态管理器 | 局内星级养成状态（替代原设计的 RogueOperatorState + OperatorRosterManager） |
| `Assets/塔防脚本/干员/OperatorStarUpgradePanel.cs` | UI | 战斗/商店场景升星面板（代码动态创建 UI，无需手动摆放） |
| `Assets/塔防脚本/干员/OperatorSelectionPanel.cs` | UI | RogueEntry 开局选择面板 |
| `Assets/塔防脚本/干员/RosterDeployInitializer.cs` | 初始化器 | 全局自动生效，把选人阵容绑定到战斗场景的 OperatorCard |

### 已修改的现有文件 ✅

| 文件路径 | 修改内容 |
|---------|---------|
| `OperatorData.cs` | 新增 maxStarRating / starPassiveDesc / selectQuote / isInitialAvailable / RegistryKey |
| `OperatorUnit.cs` | 新增 StarStatMultiplier() / RefreshStarState() / ApplyStarPassive()（7职业满星被动）/ CurrentStar 属性 |
| `OperatorCard.cs` | 新增 ApplyRosterOperator() / RefreshStarBadge() / OnStarChanged 监听 |
| `RogueRuntimeState.cs` | 新增 SelectedRoster / SetSelectedRoster / ClearSelectedRoster / TryUpgradeOperatorStar |
| `RogueEntryController.cs` | StartRun() 调用 panel.CanStart() + panel.ConfirmRoster() |
| `BalanceConfig.cs` | 新增 StarUpgradeCost / MaxStarLimit / RosterMinCount / RosterMaxCount / StarBudget / BaseStatMultiplier / StarGrowth |

### 尚未创建的文件 ❌

| 文件路径 | 类型 | 说明 |
|---------|------|------|
| `Assets/塔防脚本/干员/OperatorAcquisitionController.cs` | 控制器 | 剧情/奇遇/商店获取干员 |
| `Assets/塔防脚本/干员/OperatorRecruitPanel.cs` | UI | 商店招募分页 |
| `Assets/塔防脚本/干员/CrossActSelectionPanel.cs` | UI | 跨大局 5 选 1 |

### 尚未修改的文件 ❌

| 文件路径 | 待修改内容 |
|---------|---------|
| `DeploymentManager.cs` | 单实例检查；RetreatOperator()；OnOperatorDied()；阵亡冷却×1.5 |
| `GoldShopController.cs` | 新增招募分页 |
| `ActConfig.cs` | 新增干员配置字段（storyOperatorAcquisitions / departureReasons） |

---

## 十二、实现优先级

### Phase 1（核心功能）— 已完成 ✅
1. ✅ OperatorData 新增 maxStarRating 字段，为 42 个资产标注
2. ✅ OperatorStarRegistry 数据结构 + RogueRuntimeState 持久化（替代原设计的 RogueOperatorState）
3. ✅ 星级属性计算（OperatorUnit.StarStatMultiplier + BalanceConfig 倍率数组）
4. ✅ OperatorUnit.Start() 应用星级属性 + ApplyStarPassive() 满星被动
5. ⚠️ DeploymentManager 单实例 + 撤退/阵亡再部署冷却（撤退返还 DP 已有，阵亡冷却×1.5 未做）
6. ✅ 战斗场景动态绑定 OperatorCard（RosterDeployInitializer 全局自动生效）

### Phase 2（开局选择 UI）— 已完成 ✅
7. ✅ OperatorSelectionPanel — RogueEntry 场景干员选择面板
8. ✅ 干员头像列表（已解锁/未解锁）
9. ✅ 中间立绘预览 + 台词
10. ✅ 星数预算检查 + 已选列表（CanStart() / ConfirmRoster()）
11. ✅ RogueEntryController.StartRun() 集成

### Phase 3（获取系统）— 尚未开始 ❌
12. ❌ ActConfig 配置干员获取点
13. ❌ OperatorAcquisitionController 剧情获取
14. ❌ 商店招募分页
15. ❌ 奇遇奖励干员

### Phase 4（强化 & UI）— 已完成 ✅
16. ✅ 星级强化系统（OperatorStarRegistry.TryUpgradeStar 消耗 RunGold）
17. ✅ 升星面板 UI（OperatorStarUpgradePanel，战斗/商店自动注入）
18. ⚠️ plot 地图阵容管理面板（未做，当前仅 RogueEntry 选人）
19. ✅ 满星被动效果（7 职业全部实现）

### Phase 5（跨大局继承）— 尚未开始 ❌
20. ❌ CrossActSelectionPanel 跨大局 5 选 1
21. ❌ RogueRuntimeState.OnActCleared() 数据迁移
22. ❌ 离队剧情文案

---

## 十三、与明日方舟肉鸽的对比

| 特性 | 明日方舟 IS | 本设计 | 差异原因 |
|------|-----------|--------|---------|
| 星级 | 1~6★ | 1~5★ | 简化为 5 档 |
| 星级系统 | 固定星级 | 初始1星 + 金币强化 | 增加养成深度 |
| 队伍约束 | 希望值 | 星数预算(7) | 不引入额外货币 |
| 开局选择 | 无（固定4名） | 自由搭配 | 增加策略性 |
| 招募方式 | 希望值购买 | 剧情 + 概率 + 开局自选 | 更有叙事感 |
| 跨大局 | 无 | 选 5 名带入 | 长期策略 |
| 部署 | 单实例 | 单实例 | 一致 |
| 遗物 | 收藏品 | 天赋卡 | 战后 3 选 1 |

---

## 十四、FAQ

**Q: 为什么所有干员初始都是 1 星？**
A: 让玩家有成长空间。一个 maxStarRating=5 的干员在 1 星时已经比 maxStarRating=1 的强（×1.6 vs ×0.6），但还有 ×3.0 的成长上限。如果直接给满星，强化系统就没意义了。

**Q: 星数预算 7 会不会太少？**
A: 开局所有干员都是 1 星，7 点预算 = 最多 7 名干员。随着强化星级提升，星数会增长，但开局时 7 人已经足够应对前期。预算限制让"带几个低星工具人"还是"带少数高星核心"成为策略选择。

**Q: 天赋卡和干员为什么不混在一起？**
A: 天赋卡是"遗物"（被动加成），干员是"角色"。混在一起会削弱两种获取各自的乐趣。

**Q: 开局选的干员和剧情获得的干员有什么区别？**
A: 开局选的干员是 isInitial=true 不可放弃。剧情获得的干员可以放弃。两者都从 1 星开始，都可以强化。开局选的干员已经"永久解锁"，下次开局还可以选。

**Q: 撤退和阵亡的区别？**
A: 主动撤退返还 50% DP，冷却正常。阵亡不返还 DP，冷却 ×1.5。

**Q: 满星被动和精英 2 被动是什么关系？**
A: 满星被动 = 干员强化到 maxStarRating 时解锁的职业被动。取消了之前的"精英化"系统，改为星级强化系统，满星即解锁被动。

**Q: 放弃干员后能重新获取吗？**
A: 剧情获取的放弃后本局不再出现。商店购买的放弃后可能再次概率出现。但解锁状态永久保留，下次开局可以选。
