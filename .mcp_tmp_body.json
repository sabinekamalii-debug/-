{"jsonrpc": "2.0", "id": 92888, "method": "tools/call", "params": {"name": "execute_code", "arguments": {"action": "execute", "code": "var sb = new System.Text.StringBuilder(); var all = Resources.LoadAll<OperatorData>(\"\"); foreach (var d in all) { if (d == null || d.unitPrefab == null) continue; var srs = d.unitPrefab.GetComponentsInChildren<SpriteRenderer>(true); int minOrder = 9999, maxOrder = -9999; foreach (var s in srs) { if (s.sortingOrder < minOrder) minOrder = s.sortingOrder; if (s.sortingOrder > maxOrder) maxOrder = s.sortingOrder; } sb.AppendLine(d.operatorName + \" | min=\" + minOrder + \" max=\" + maxOrder); } sb.AppendLine(\"--- enemies ---\"); var enemies = Resources.LoadAll<GameObject>(\"人物/敌人\"); foreach (var e in enemies) { if (e == null) continue; var srs = e.GetComponentsInChildren<SpriteRenderer>(true); int minOrder = 9999, maxOrder = -9999; foreach (var s in srs) { if (s.sortingOrder < minOrder) minOrder = s.sortingOrder; if (s.sortingOrder > maxOrder) maxOrder = s.sortingOrder; } sb.AppendLine(e.name + \" | srs=\" + srs.Length + \" min=\" + minOrder + \" max=\" + maxOrder); } return sb.ToString();"}}}