

## Codely Structured Memories

### User

### Feedback
- [2026-07-27 16:55:55] Operator card position is fixed on the right side at the "角色商店处" (SpriteRenderer at world pos ~7.88, 0, 0). DO NOT change the card layout/position/anchors — only one 先锋 (先锋测试, cost=6) should be active in the scene. **Why:** user explicitly said cards must stay in the right-side purchase area and not be moved/repositioned. **How to apply:** when modifying operator cards, only change individual card properties (cost/sprite/active), never the Canvas render mode, card positions, parent hierarchy, or layout.
- [2026-07-29 15:31:38] [feedback] When creating TMP text components via AddComponent/new GameObject, always use typeof(TextMeshProUGUI), never typeof(TMP_Text) — TMP_Text is abstract and will throw at runtime, silently breaking the calling method. **Why:** BattleEventManager.CreateDefaultPanel used typeof(TMP_Text), threw an exception mid-method, returned null, left a visible black Canvas on screen during battle. **How to apply:** any runtime UI creation with TMPro text fields.
- [2026-07-29 18:36:46] User wants enemy waves to be very easy to defeat during development/testing phase. When configuring WaveData, use basic enemy types (Enemy/GeBuLin/小骷髅) with low counts (2-4 per wave) and slow spawn intervals (2.5-4s). **Why:** user is in dev testing stage, needs to quickly clear levels to verify functionality. **How to apply:** all level wave data configuration during current dev phase.
- [2026-07-29 18:59:04] When fixing level scenes to match level 1, the correct approach is to directly copy entire UI objects (遭遇战菜单/结束菜单/角色画布/敌我信息canvas/角色商店处) from level 1 via additive scene load + Instantiate, NOT to manually recreate them component-by-component. Manual recreation misses Canvas sortingOrder, anchoredPosition, sprite references, and child hierarchies. **Why:** user explicitly said "直接复制粘贴取缔覆盖", manual approach wasted many iterations on sprite/position/sortingOrder mismatches. **How to apply:** all level scene standardization work.
- [2026-07-30 08:43:45] [feedback] User prefers clean card list rows — only icon + card name, NO type/rarity tags or description preview text in the list. Descriptions should only appear in the hover/preview popup. **Why:** gray small text in each row causes visual fatigue; user explicitly asked to remove it. **How to apply:** when designing card/item list rows, keep each row minimal (icon + name only); put all detailed info in a separate preview panel on hover.
- [2026-07-30 13:40:15] [feedback] Two UI issues on Tuanjie 1.9.1 + URP: ① InputSystemUIInputModule (1.7.0) doesn't dispatch scroll wheel events to ScrollRect — scrollWheel InputAction fires but module never delivers to IScrollHandler. Manual Mouse.current.scroll reading didn't work for user either. Fix: hold-left-button drag-scroll in Update() (track Mouse.current.leftButton.isPressed + position delta, clamp content.anchoredPosition). ② UnityEngine.UI.Mask (stencil) doesn't clip in URP — ScrollRect content overflows viewport visually. Fix: replace Mask with RectMask2D on viewport. **Why:** Tuanjie/URP/InputSystem compatibility bugs. **How to apply:** any ScrollRect in this project: use RectMask2D (not Mask) for clipping; implement manual drag-scroll if wheel doesn't work.
- [2026-07-30 21:30:38] VideoSceneLoader.LoadSceneRoutine had a bug: SetLoadingVisible(true) and SceneManager.LoadSceneAsync were called in the same frame, so the loading UI never rendered before heavy scene loading blocked the main thread. Fix: show loading UI first, then `yield return null` to let Canvas render, then start async load. Also moved RogueRuntimeState.SaveRunStateIfNeeded() after the yield. transitionMinShowSeconds increased from 0.35s to 0.6s. **Why:** user reported clicking "开始本局" caused a freeze with no loading UI, then UI flashed briefly right before scene switch. **How to apply:** any scene transition using VideoSceneLoader — always ensure loading UI has at least one frame to render before starting heavy async operations.
- [2026-07-31 08:08:41] Naninovel TitleUI Canvas sortingOrder=100 会覆盖 StoryCardCollection 的 UIRoot Canvas（sortingOrder=0）。在 Play 模式下可用 execute_csharp_script 运行 GameObject.Find("TitleUI").SetActive(false) 临时隐藏验证下层 UI，这不影响 Naninovel 配置。**Why:** 需要在 StoryCardCollection 场景直接运行时绕过 Naninovel TitleUI 遮挡。**How to apply:** 调试 StoryCardCollection 场景时如需看 UIRoot 下的 UI，临时隐藏 TitleUI 即可。
- [2026-07-31 08:33:37] 调试 Naninovel 问题时不要随意修改 ResourceProviderConfiguration（UseAddressables/AllowAddressableInEditor）或 Addressable PlayMode 设置。这些配置与用户的 Addressable build 缓存相关联，修改后可能导致加载链断裂。修改后必须恢复原始值。**Why:** 用户之前能正常使用，说明配置本身没问题，问题通常在数据层（scriptName 错误、Addressable entry 缺失等）。**How to apply:** 先检查数据引用和 Addressable 注册，最后才考虑配置问题；修改配置后务必恢复。
- [2026-07-31 13:42:12] [2026-07-31] Emoji 字符（如 🔥📖⚒✨🔪💰🃏❤☠）在 TMP 文本中会显示为 tofu（□），此项目的 TMP 字体不支持 emoji。**Why:** 用户截图后发现多个按钮标题出现 □ 方块。**How to apply:** 在 TMP 文本组件中不要使用 emoji，用纯文字描述即可（如"休整"而非"🔥 休整"）。
- [2026-07-31 14:09:04] [2026-07-31] 用户设计的关卡航点路线是最终设计成果，不能被覆盖或用 A* 重新生成替代。修复穿墙问题的正确方法是改地图（沿路线打通墙壁格子为地面），而不是改路线。**Why:** 用户说"敌人路线都给我变了""都是我设计好的"，A* 重新生成的路线完全偏离了原始设计意图。**How to apply:** 任何涉及 LevelConfig pathNWaypoints 的修改，必须从旧关卡场景重新导出原始航点数据，不能自动生成替代。地图 gridData 应配合路线调整，而非反过来。
- [2026-07-31 19:23:47] [2026-07-31] 编辑器工具必须有 Undo 支持。用户改乱数据后无法 Ctrl+Z 恢复，导致数据丢失。**Why:** 自定义 EditorWindow 默认不接入 Unity Undo 系统，用户期望 Ctrl+Z 能撤销所有编辑操作。**How to apply:** 自定义编辑器中所有修改 ScriptableObject 数据的操作前必须调用 Undo.RegisterCompleteObjectUndo(obj, label)，并在工具栏提供撤销/重做按钮。
- [2026-08-01 07:33:59] [feedback] User's explicit preferences for automation scripts: ① No `while($true)` infinite loops — unstable, memory leaks, breaks on sleep/wake/update. Use Windows Task Scheduler to launch every 10 min, run once, exit. ② Don't use `git add -A` — too dangerous. Use `git add .` + check for dangerous staged dirs (Library/Temp/Logs/Obj/Build) and abort if found. ③ Commit messages: "Auto Backup YYYY-MM-DD HH:MM\nChanged: N files" — not generic "auto:". ④ Push failures: log and wait for next scheduled run, don't retry aggressively. ⑤ Daily `git bundle` at 22:00 to `D:\GitBackups\` as secondary backup. **Why:** user's past experience with long-running PS scripts. **How to apply:** any automation/scheduling task for this project.
- [2026-08-02 12:11:21] [feedback] GameMode 下拉框实现要点：① 锚点必须用 (0,1) 与其他按钮一致；② TMP_Dropdown 下拉列表项在运行时通过 DropdownDescSetter（LateUpdate 轮询 + Title 文字匹配）设置右侧描述文字，Template 比按钮宽(520px)以容纳描述，模板有半透明深色背景(0.08,0.06,0.12,0.92)保证可读性；③ 按钮文字用静态 Caption 子对象手动更新显示"模式选择：当前模式名"，captionText=null；④ 描述文字必须极简（4字：每关固定/固定+随机/每关随机），不要长句。**Why:** 经历多次布局/文字长度调整。**How to apply:** 修改此下拉时保持以上要点。

- [2026-08-01 19:18:45] [2026-08-01] RectMask2D 在 Scene View 编辑模式下也会裁剪 Content 内容——不仅是 Game View。plot.unity 的 ScrollView 有16个关卡按钮，但 Viewport 上的 RectMask2D 只显示视口内的3个，其余被隐藏。编辑时临时禁用 RectMask2D 组件即可看到全部内容，运行游戏前再启用。**Why:** 用户发现在 Scene View 只能看到3个关卡按钮，其余不可见。**How to apply:** 当 ScrollView Content 内容在 Scene View 中不可见时，检查 Viewport 上的 RectMask2D/Mask 是否在裁剪，编辑时临时禁用即可。
- [2026-08-01 19:34:26] [feedback] 设计文档中"节点之间穿插：奇遇/商店/休息（不计入战斗节点）"的意思是：21个节点就是地图上的全部按钮，穿插事件是节点之间的弹出事件，不是额外的按钮。不要把穿插节点加成额外的地图按钮。**Why:** 之前AI误解为28个总节点（21战斗+7穿插按钮），被用户纠正。**How to apply:** 修改关卡数量时，节点数=战斗节点数，穿插事件通过弹窗系统实现，不占按钮位置。
- [2026-08-01 21:52:54] [feedback] Resources.LoadAll<T>("folder") 递归加载子目录——把不想加载的资产移到 Resources/ 下的子文件夹（如 _disabled）不会阻止加载，仍然会被全部加载。必须移到 Resources/ 文件夹外面才能排除。**Why:** 之前把20个旧RandomEvent资产移到 Resources/RandomEvents/_disabled/ 子目录，结果 Resources.LoadAll<RandomEventData>("RandomEvents") 仍然加载了全部21个。**How to apply:** 临时排除 Resources 下的资产时，移到 Assets/ 根目录下的其他文件夹，不要放在 Resources/ 的子目录里。
- [2026-08-01 21:57:01] [feedback] 用户要求清理项目中所有 Debug.Log 和 Debug.LogWarning，只保留 Debug.LogError。**Why:** 控制台日志太多，重要的错误信息被淹没。**How to apply:** 后续写代码时不要添加 Debug.Log/LogWarning；如需调试用 Debug.Log，调试完后立即删除；保留 Debug.LogError 作为真正的错误提示。
- [2026-08-02 07:20:30] [2026-08-02] StoryCardButton 预制体只有一个 Image（按钮自身的背景图），TryAutoBind() 会把 iconImage 绑到这个背景图上。锁定/解锁逻辑不能清 iconImage.sprite=null，否则背景图消失变白。修复方式：锁定时只改背景颜色，不清 sprite；解锁时只在 data.icon 有值时才替换 sprite。**Why:** 预制体没有单独的 Icon 子对象，iconImage 和背景图是同一个 Image。**How to apply:** 修改 StoryCardButton 锁定/解锁外观时，不要动 iconImage.sprite。
- [2026-08-02 12:11:04] [2026-08-02] [feedback] GameMode 下拉框描述文字要简短，每项4个字即可（如"每关固定"/"固定+随机"/"每关随机"），不要长句描述。**Why:** 用户觉得之前的长描述文字太多太冗余。**How to apply:** 修改下拉框描述文字时保持极简。
- [2026-08-02 21:38:41] [feedback] 用户让我改杀塔2风格分叉路径地图时，我只应改关卡路径算法结构（MapGraph/StSMapGenerator/LevelNodeButton等代码层），绝不能动场景里已有的 UI 布局（ScrollView位置、HeldCardsFrameRoot、StoryCardPanelRoot等）。**Why:** 我擅自移动了 HeldCardsFrameRoot 和 ScrollView 的锚点/位置，用户发现后很生气"你干什么？我只是让你改线性结构，你怎么把我的UI删了"。**How to apply:** 修改功能时严格限制在用户要求的范围内，不动任何场景中已有的 UI 布局；动态生成的 UI 元素应在现有区域内适配，不能挤占其他面板空间。
- [2026-08-02 22:29:39] [2026-08-02] 动态创建 RectTransform 时必须设置 anchor 和 pivot 匹配父级 Content 的 pivot，否则 anchoredPosition 坐标基准不对齐。**Why:** Content pivot=(0.5,1.0) 但节点 anchor 默认 (0,0) 导致线对不上节点位置。**How to apply:** 所有通过代码创建的 UI 子物体（节点/连线等），必须显式设置 anchorMin/anchorMax/pivot 匹配 Content 的 pivot。
- [2026-08-03 10:47:31] [2026-08-03] 编辑器模式下必须能看到所有 UI 效果（背景图、节点、连线等），不能只在 Play 模式运行后才显示。所有动态生成的 UI 必须通过 Tools 菜单在编辑模式下也能生成并正确显示。**Why:** 用户多次抱怨"又必须运行才能看到""不要这种代码控制的运行才变化的"，编辑模式下看不到效果就无法调整布局。**How to apply:** 动态生成的 UI 元素必须有对应的编辑器工具（如 Tools 菜单项），生成后立即在 Scene View 和 Game View 中正确显示；MapBackground 等背景图不能用 sizeDelta.x=0 导致宽度为 0 不可见。
- [2026-08-03 18:23:25] [2026-08-03] 用户决定右侧干员面板保持现状（可滚动+卡片缩小方案），不改到底部。原因：地图按上下填满设计，移到底部需要改全部30个LevelConfig的gridData+路径+守护点位置，工作量过大。**Why:** 地图20×12网格在摄像机orthoSize=5下已填满垂直方向，底部无余量。**How to apply:** 干员面板布局改动限于右侧区域内优化（滚动+缩小），不涉及地图数据。
- [2026-08-03 18:24:12] [2026-08-03] 干员卡片设计方向：用户确认用方案C（AI生成头像），但AI生成服务当前401无权限，临时用已有素材分配。用户选择商店招募入口为Tab切换（天赋卡/干员招募两个Tab），卡片展示为极简风格（头像+名字+星级+费用），OperatorData.icon已有sprite可直接使用。干员卡牌头像生成提示词文档在Assets/游戏设计文档/干员卡牌头像生成提示词.md。
- [2026-08-05 19:22:08] [feedback] 生成角色立绘图片时必须使用透明背景（is_segmentation: true），不要纯白背景。提示词文档中写的"白色背景"不用管，一律生成透明背景。**Why:** 用户明确要求立绘需要透明背景以便直接用于游戏。**How to apply:** 所有 generate_image / generate_sprite 调用都设 is_segmentation=true。
- [2026-08-08 10:13:05] [updated] AI生成走路/战斗动画的可行方案已验证成功：①用AI视频生成走路/战斗视频 → Python cv2 提取全部帧 → numpy 灰度帧对比找循环周期（frame 0 vs 所有后续帧，diff最低点=周期长度）→ 每隔3帧取样约11帧 → rembg+birefnet-general 只对选中帧抠透明背景（不全帧抠图省时间）→ Unity AnimationClip PPtr曲线生成循环动画。关键：帧率8fps；AnimationClip设loopTime=true；末尾关键帧用首帧值实现无缝循环（不是重复添加首帧关键帧）；外部脚本写文件到 Assets/ 会被 Refresh 静默删除，必须先写项目外临时目录再通过 C# 复制进来。**Why:** AI直接生成序列帧/图片无法控制腿部运动且对四足动物无效，但视频模型能生成全身动作，拆帧后可用。**How to apply:** 需要角色动画时用此流程；战斗帧选取视频前30帧（动作最稳定段）；走路帧用帧对比找周期后取样。


- [2026-08-06 21:56:25] [feedback] AI序列帧(sprite_sequence)的.anim文件在Tuanjie引擎Animation窗口中只显示2个蓝点（PPtr曲线显示问题），但实际25帧关键帧都在，播放正常。不要被Dopesheet显示的2个关键帧误导。**Why:** Tuanjie 1.9.1的Animation窗口对PPtr（精灵换帧）曲线显示不完整。**How to apply:** 检查序列帧动画是否正确时，直接播放看效果，不要看Dopesheet关键帧数量。
- [2026-08-08 09:07:38] [2026-08-07] 处理批量走路帧动画时，绝不能"全帧直接循环"抄近道。必须逐帧分析import batch，判断每帧的走路姿态 [contact-left/contact-right/passing/static]，剔除 static 站立帧、筛选 contact+passing 交替的连续段，只保留真实走路段重建 AnimationClip。**Why:** 用户明确抱怨"走路帧根本不连贯首尾不和谐"，因为AI视频拆帧里混入大量静态站立/摆pose帧，还有角色全是静态素材（walk_14/18/19/20/04/05用视觉逐帧确认几乎全static，根本无走路动作，不可救）。**How to apply:** 生成走路/跑步动画时，逐帧标注姿态、删静止帧、保证首尾帧姿态衔接;对纯静态素材要诚实告知用户"无法重排出走路"，改用真走路重生成或伪走路(Transform弹性)而非强行循环。
- [2026-08-08 09:08:08] [2026-08-08] sprite_sequence 工具对四足动物角色不适用——AI 模型是人形训练的，会生成人形走路动作而非四足步态。战斗帧(idle)生成结果30帧几乎完全重复无变化。**Why:** 用户明确反馈"生成的效果非常不好，这个是个豹子你生成的走路帧却是按照人的走路生成的，战斗帧30个图片完全一模一样"。**How to apply:** 四足动物角色动画不要用 sprite_sequence，改用 AI 视频生成→拆帧→rembg 抠图流程。视频生成时 prompt 必须强调 "full body visible, camera stays still, no zoom, no camera movement, wide shot" 防止镜头推进导致裁切。视频用 16:9 横屏给全身更多横向空间。
- [2026-08-08 10:12:55] [2026-08-08] 外部脚本（Python/cv2/rembg）直接往 Assets/ 子目录写 PNG 文件后调用 AssetDatabase.Refresh()，Unity 会静默删除没有 .meta 的文件——战斗帧文件夹两次被清空。**Why:** Unity 导入器在 Refresh 时对无 .meta 的文件处理不一致，有时生成 .meta 有时直接删除。**How to apply:** 外部脚本生成帧图片时，先写到项目外临时目录（如 D:\unity\mowang\temp_xxx），筛选/抠图完成后用 execute_csharp_script 通过 File.Copy 复制到 Assets 内并立即 Refresh，让 Unity 一次性生成 .meta。
- [2026-08-08 14:45:44] [feedback] generate_sprite_sequence 工具不适合四足动物角色（豹/狼/虎等）。AI模型是人形训练的，对四足动物生成结果：①走路帧会变成人形走路动作；②idle帧大量重复（30帧只有2-3种不同图片）。**Why:** sprite_sequence_v1 模型仅理解人形运动。**How to apply:** 四足动物角色动画必须用视频生成方案（generate_video → cv2拆帧 → rembg birefnet-general抠图 → 筛选帧 → AnimationClip），不能用 generate_sprite_sequence。2026-08-08影豹（黑豹）验证：sprite_sequence结果废弃，视频方案拆帧后四足行走/战斗姿态正确。
- [2026-08-09 08:49:59] [2026-08-09] Tuanjie Animation 窗口无法在两帧之间插入非网格时间点的关键帧——指针只能吸附到 Samples 设定的帧格点。脚本写入的非网格时间（如 0.25s 在 samples=2 时）会被引擎忽略或覆盖，导致动画速度变慢而非变快。**Why:** 多次尝试通过脚本修改单帧间距（第一帧到第二帧快一倍、其他不变）都失败，用户反复反馈"改反了""还是慢"。**How to apply:** 不要尝试通过脚本单独修改某两帧之间的时间间距。如需非均匀帧间距，只能：(1)统一改 Samples（但会影响所有帧速度），(2)在 Animation 窗口手动操作（但 Tuanjie 不支持在格点间放置），(3)接受所有帧统一速度。RGBA mode 不代表透明背景——alpha 可能全是 255，需实际检查像素或用 rembg 抠图。
- [2026-08-09 13:22:03] [2026-08-09] 绑定走路动画到 `111` 对象时，必须同时执行三步：①设置 SpriteRenderer.sprite 为首帧 walk_001；②设置 Animator.runtimeAnimatorController 为新 controller；③设置 Animator.enabled = true。漏掉任何一步都会导致旧角色动画残留。**Why:** 傀儡师绑定时只设了 controller 没换 sprite 也没 enable animator，导致场景里还显示紫雷狼的 walk_003 精灵，用户截图反馈"没顶替完整还是原来的动画"。**How to apply:** 每次 execute_csharp_script 绑定动画时，三步缺一不可，最后截图验证。注意：Tuanjie 引擎中 Animator 有时在保存场景后 enabled 会变回 false，可靠修复方式是先 DestroyImmediate 旧的 Animator 组件再 AddComponent 新的，而非仅设 enabled=true。死囚和魔物法师绑定时都遇到此问题，销毁重建后稳定。
- [2026-08-09 14:13:35] [feedback] 人形角色走路动画必须直接用 generate_sprite_sequence 生成，不要用"视频生成→拆帧→rembg抠图"流程。**Why:** 视频拆帧后rembg抠图经常丢失角色面部细节（"搞一堆没有脸的玩意"），且整体效果差，用户两次删除结果。用户明确说"你就不能直接生成走路帧图片吗非得用视频剪切，我记得你能直接生成的啊"。**How to apply:** 人形角色用 generate_sprite_sequence（frontRun/idle/backRun），四足动物才用视频方案。另外用户可能已经自己抠好了透明背景图（image.png），先检查是否已有透明PNG再决定是否需要rembg。
- [2026-08-09 18:54:56] [2026-08-09 15:30:00] [feedback] 创建 AnimatorController 后必须验证 states 数量。Tuanjie 引擎中 AnimatorController.CreateAnimatorControllerAtPath 后 AddState 有时静默失败，导致 controller.layers[0].stateMachine.states.Length=0、defaultState=NULL，Animator layerCount=0 不播放动画。**Why:** 猎魔人绑定后用户反馈"没动画"，检查发现 controller 有 layer 但无 state。**How to apply:** 绑定脚本中创建 controller 后必须 Debug.Log 验证 sm.states.Length>=1 且 defaultState!=null，如为 0 则删旧 controller 重建。Post-save 还需验证 animator.layerCount>=1。
- [2026-08-10 14:32:28] [2026-08-10] 魔族角色头像(sourcePath)和预制体SpriteRenderer图片是两组不同的图片，用户分别指定。预制体SpriteRenderer用全身立绘/展示图（第一组），头像Sprite用另一组图片（第二组）。**Why:** 用户明确纠正"这个是预制体图片不是头像""头像你先用这些凑合"。**How to apply:** 以后配置角色时，区分预制体展示图和头像图，不要混用或自行选图，必须按用户指定的对应关系赋值。
- [2026-08-10 18:29:32] [feedback] 排查UI视觉问题（如"黄色遮挡"）时，应尽早检查TMP文本组件的内容和颜色，不要只盯着Image/SpriteRenderer。**Why:** 用户报告右侧商店有黄色遮挡，我花了大量工具调用检查Sprite像素、Image颜色、TextureImporter、全场景SpriteRenderer列表，最后才发现是13张卡片的"费用文字"TMP显示"???"且颜色为亮黄色(1.0,0.92,0.02)，宽度1.9几乎满铺卡片，13张叠在一起形成黄色竖条。**How to apply:** 遇到"不明色块遮挡"类问题时，第一步就应列出目标区域内所有UI组件（含TMP文本）的内容、颜色、尺寸，而不是先逐个检查Sprite像素值。
- [2026-08-10 18:55:46] [2026-08-10] WorldSpace Canvas（renderMode=WorldSpace, localScale=1）上的 TMP 文本组件 fontSize 必须用世界单位小数（如 0.2/0.25/0.3），不能用像素整数值（如 14/22）。**Why:** 用了像素值导致文字比卡片大3倍，文字竖排溢出遮挡整个面板，浪费大量调试迭代。**How to apply:** 在 WorldSpace Canvas 上创建 TMP 文本时，fontSize 设为世界单位小数；enableWordWrapping=false 防止多字名折行成单字竖排。
- [2026-08-10 18:55:46] [2026-08-10] 截图 BattleScene 时结束菜单(Canvas, sortingOrder 未设但 scale=0.009259) 默认激活会遮挡右侧商店面板，导致截图看不到商店。调试商店 UI 时需先 SetActive(false) 结束菜单再截图，完成后恢复。**Why:** 结束菜单的抽卡/颁奖台 UI 叠在商店区域上方。**How to apply:** 需要截图查看干员商店面板时临时禁用结束菜单。
- [2026-08-14 19:39:54] 部署栏/商店动态建卡时，除了必须用具体类 TextMeshProUGUI 而非抽象 TMP_Text，还【必须】给每张卡设置纵向 anchoredPosition——否则所有卡落在 Content 原点完全重叠，viewport 里只露一张，用户看到的症状是"只显示1张卡+名字异常"。2026-08-14 修复 RosterDeployInitializer.BuildCards：按 y=-1.1-index×2.35 堆叠（复用 BattleScene 干员商店面板 cardHeight=2.2+spacing=0.15，锚点(0.5,1)/pivot(0.5,0.5)），名字 fontSize=0.5/费用=0.55 用世界单位小数。子物体名"名字"/"费用文字"是 RefreshNameAndCost 的查找约定，不能改名。RosterDeployInitializer 用 RuntimeInitializeOnLoadMethod(AfterSceneLoad) 监听 sceneLoaded，但对首个直接打开的 BattleScene 不会触发（首场景加载发生在注册订阅前）。

### Project
- [2026-07-26 07:50:20] Project convention: UI controllers (RandomEventController, GoldShopController, etc.) use `GameObject.Find("name")` runtime lookup instead of serialized field references for UI binding. When creating scenes for these controllers, GameObject names MUST match exactly: EventTitle, EventDescription, GoldCount, Btn_Leave, EventBackground, OptionsList.
- [2026-07-26 07:50:22] Pre-existing compile errors from Naninovel package (PlaceholderCharacterMetadata, PlaceholderBackgroundMetadata missing types) are NOT caused by user code — ignore them when checking compilation results.
- [2026-07-26 08:08:51] [project] Shop system redesigned with S1-S4 roguelike mechanics: S1 random shop slots (4-6 cards, rarity-weighted draw from unowned pool), S2 slot discounts (~30% chance, 0.7-0.9x multiplier), S3 paid refresh (10/15/20... escalating cost), S4 card removal service (25/40/55... cost, 1 per shop visit). Card type possession limit = 3 per TalentCardType. All constants in BalanceConfig.cs, shop state in RogueRuntimeState.cs, UI in GoldShopController.cs, card pool logic in GoldShopConfig.cs.
- [2026-07-26 20:32:50] [project] Test battle scene "level test" created at Assets/Scenes/level/level test.scene with GridSystem + LevelGridGenerator + LevelGridConfig_Test.asset (16×10 symmetric layout). Tiles are procedurally generated from text config (.=empty, G=ground, W=wall, H=highground). **Why:** user wants to experiment with map tile appearance and layout in an isolated scene before applying changes to production levels. **How to apply:** use this scene as the sandbox for map/tile system improvements; modify LevelGridConfig_Test.asset rows and right-click LevelGridGenerator → "Generate From Config" to regenerate tiles.
- [2026-07-27 16:55:55] Cost text display bug: all 先锋 operator cards showed "$45" as cost text regardless of actual OperatorData.cost (which ranges 3-9). The cost TextMeshPro text is manually set and not auto-synced to OperatorData.cost. When adding operator cards, the cost text must be manually set to match the data.
- [2026-08-02 09:06:47] [CORRECTION] `.scene` IS the native scene format for Tuanjie engine (not a duplicate of .unity). The previous advice to add `*.scene` to .gitignore was WRONG — it caused GoldShop.scene, RogueEntry.scene, SoulShop.scene, and level test.scene to be untracked, losing scene data on branch switches. `.scene` and `*.scene.meta` rules have been REMOVED from .gitignore. All 4 .scene files are now tracked in git.

- [2026-07-29 13:18:58] [project] LeftPanel (old StoryCardPanel) deleted from plot.unity. New StoryCardPanelRoot created under Canvas (inactive by default) with StoryCardPanel component, wired to switch with HeldCardsFrameRoot via BtnStoryCard button. Switching logic: HeldCardsFrame.ShowStoryCardPanel() hides HeldCardsFrameRoot + shows StoryCardPanelRoot; StoryCardPanel.ReturnToHeldCards() does the reverse. RogueCollectionController guard changed from FindFirstObjectByType<StoryCardPanel>() to FindFirstObjectByType<HeldCardsFrame>(). LevelEndMenu no longer auto-activates StoryCardPanel on level win (only calls Refresh). **Why:** user wanted to eliminate visual overlap of two left-side panels. **How to apply:** StoryCardPanelRoot must stay inactive by default; it's activated only via the BtnStoryCard button on HeldCardsFrameRoot.
- [2026-07-29 13:22:49] User tests by directly running the plot scene in Unity Editor (no Title → Start Game flow). This means ForceResetRun() is never called via normal game flow during testing. LevelMapController.Awake() now checks LevelSceneLoadContext.Peek() — if null, it clears LevelProgress + HasActiveRun. This is the correct approach for detecting direct-scene-run testing.
- [2026-07-29 14:11:29] [project] TalentCardRarity field (Common/Advanced/Rare/Legendary) is a 废稿 — all 96 card assets have rarity=0 (Common) except rar_vanguard_speed (rarity=2). User never designed rarity tiers; only TalentCardType (6 types: Special/Attack/Defense/Guardian/Rare/Skill) is meaningful. **Why:** rarity was part of an early design that was never adopted. **How to apply:** do not display or reference TalentCardRarity in UI/logic unless explicitly asked; treat TalentCardType as the only card classification.
- [2026-08-10 18:48:03] Level scene required components checklist (derived from level 1 as golden template): each level scene MUST have: ① GridSystem.defensePoint assigned (not null — breaks deployment if missing), ② Spawner.waves array with no duplicate WaveData entries, ③ Spawner with all ObjectPooler refs (enemyPool/smallBossPool/bigBossPool/goblinPool/skeletonPool/smallSkeletonPool/darkKingPool), ④ Grid with Ground/Wall/HighGround Tilemaps + tiles painted, ⑤ Path objects (Path0-Path3) with WayPoints, ⑥ 守护点 with DeployLightController + DefensePointShooter, ⑦ UI: 敌我信息canvas/遭遇战菜单/结束菜单/角色画布, ⑧ Managers with Global Light 2D + Main Camera, ⑨ EventSystem. **Note:** 旧"角色商店处" SpriteRenderer 已从 BattleScene 删除(2026-08-10)，可滚动干员面板(角色画布>干员商店面板)已完全替代它。**Why:** level 2 had defensePoint=null and duplicate Wave 2 in Spawner.waves. **How to apply:** use this checklist when fixing or creating any level scene; 角色商店处 不再需要。

- [2026-07-29 18:59:04] Level 2 fully standardized to match level 1 as of 2026-07-29. Copied 5 root objects from level 1: 遭遇战菜单(inactive+scale0), 结束菜单(抽卡/查看剧情碎片/返回主菜单+卡片+颁奖台), 角色画布(5 active operator cards), 敌我信息canvas (1)(with 文本对话 system), 角色商店处. Also fixed: GridSystem.defensePoint→守护点, Spawner waves deduplicated(9→8), all WaveData simplified to basic enemies(Enemy/GeBuLin/小骷髅, 2-4 per wave). **Why:** level 2 is the first level to be standardized as template for all other levels. **How to apply:** use level 2 as reference when standardizing levels 3-17.
- [2026-07-30 08:09:32] GoldShop 删卡服务 redesigned: main shop shows only a "进入删卡服务" button; clicking it opens a separate full-screen overlay panel (RemovePanel) with dark opaque background, showing owned cards with "删除" buttons. Panel has "← 返回商店" button to return to main shop. **Why:** user wanted a clean shop UI where deleting cards is a separate mode, not mixed with buyable items. **How to apply:** when adding new shop services, use separate overlay panels rather than inline card lists.
- [2026-07-30 08:56:54] [project] HeldCardsFrame UI overhauled (2026-07-30): panel widened to 600px, filter bar (全部/普通/进阶/稀有/传奇) completely removed (rarity is 废稿), card rows show only icon(100px)+name(32pt). Hover shows large centered card preview (420×680, icon 340px, name 40pt, desc 28pt, effects 26pt). StoryCardPanelRoot also widened to 600px. HeldCardsFrameBuilder.cs updated to no longer generate FilterBar. Viewport offsetMax = (-4, -52) to sit below Title only.
- [2026-07-30 18:47:34] [2026-07-30] 奇遇(RandomEvent) × Naninovel 集成已就绪。evt_hermit.asset 配置了 playNaniOnEnter=true + naniEntryScriptName="奇遇测试 1"，进场自动触发 Naninovel 剧本；选项1"接受考验"也配置了 naniScriptName="奇遇测试 1"。空之魔王角色有12张表情图片。RandomEventController.Debug.Log 已全部清理，仅保留2处 Debug.LogWarning（看门狗+回退）。**Why:** 用户要求奇遇关卡绑定 Naninovel 剧情，之前的AI已搭建完整基础设施但Debug.Log刷屏。**How to apply:** 如需配置更多奇遇事件，在 RandomEventData 资产中设置 playNaniOnEnter/naniEntryScriptName 和选项的 naniScriptName/naniLabel 即可。
- [2026-07-31 07:17:32] BattleScene.unity is now the unified data-driven battle scene, replacing the old per-level scene architecture. It contains all root objects copied from level 1 (Grid with pre-painted tiles, Spawner with 7 ObjectPooler children, 守护点 with DeployLightGroup, UI canvases, etc.) plus BattleSceneBootstrap + MapBuilder for runtime level config injection. **Why:** another AI's attempt to create this unified scene failed — all transforms/components/tiles were broken. Fix was to copy level 1's objects directly and rewire references. **How to apply:** when modifying BattleScene, always ensure Spawner.enemyPools is populated (7 entries: Enemy→EnemyPool, Smallboss→smallpool, Bigboss→bigpool, GeBuLin→哥布林, KuLou→骷髅, 小骷髅→小骷髅, 空之魔王→黑之魔王). BattleSceneBootstrap.useDefaultConfigForTesting must stay false to prevent scene data being overwritten.
- [2026-07-31 08:08:36] StoryCardCollection.unity 场景有三个面板同时激活：StoryCardPanelRoot（正式卡片网格）、StoryMirrorPanelRoot（碎裂之镜）、CollectionController（旧 RogueCollectionController）。StoryMirrorPanelRoot 已禁用，RogueCollectionController 已加守卫（检测到 StoryCardPanel 存在时跳过 Refresh）。**Why:** 三套 UI 叠加导致文本重叠、布局混乱。**How to apply:** 修改 StoryCardCollection 场景时，只用 StoryCardPanelRoot 作为卡片展示面板，不要重新启用 StoryMirrorPanelRoot。
- [2026-07-31 08:08:39] StoryCardCollection.unity UIRoot Canvas 已改为 ScreenSpaceOverlay（原为 ScreenSpaceCamera，scale 被算成 0.11 导致整个 UI 缩到极小）。CanvasScaler 改为 ScaleWithScreenSize 1920x1080 match=0.5。StoryCardPanel.cardContainer 已绑 CardGridRoot，pageText 已绑页码文本。**Why:** 原配置导致 UI 不可见。**How to apply:** 不要把 UIRoot Canvas 改回 ScreenSpaceCamera，除非有专门的 worldCamera 配置。
- [2026-07-31 08:09:02] 已通过 PlayerPrefs 永久解锁 8 张剧情碎片用于测试：gucheng_01/02/03、liehen_01/02/03、weizhuang_01/02。清除方式：PlayerPrefs.DeleteKey("StoryCard_UnlockedIds")。**Why:** 用户要求强制解锁几张卡片方便查看场景效果。**How to apply:** 如需重置解锁状态，删除该 PlayerPrefs key。
- [2026-07-31 08:33:13] Naninovel 脚本加载机制：IScriptPlayer.Play() 只播放已加载的脚本，不会触发加载。实际游戏流程中 NaninovelReturnAutoPlayer 调用 LoadAndPlay()（扩展方法），先通过 IScriptLoader.Load() 加载脚本资源，再调用 Play() 播放。调试时不能直接用 player.Play() 测试，必须先 Load。**Why:** Play() 内部调用 GetLoadedOrErr()，脚本未加载时直接抛异常。**How to apply:** 调试 Naninovel 脚本播放问题时，用 IScriptLoader.Load() 先加载，或直接用 LoadAndPlay() 扩展方法。
- [2026-07-31 08:33:17] 20 张 StoryCardData 的 scriptName 已全部改为已有的 Naninovel 脚本（魔王 1~8）。映射规则：每个套系 4 张卡循环映射到魔王 1-4 或 5-8。原 scriptName（gucheng1/liehen2/weizhuang1 等）对应的 .nani 文件从未创建过。**Why:** 卡片点击后切到 Title 场景但 Naninovel 找不到脚本，导致空白。**How to apply:** 如需为每张卡创建独立剧情，需在 Assets/Scenario/ 下创建对应的 .nani 文件并加入 Addressable group（address=Naninovel/Scripts/{name}）。
- [2026-07-31 08:33:31] Naninovel 配置：UseAddressables=true, AllowAddressableInEditor=false, Addressable PlayMode=Use Existing Build (index 2)。.nani 文件在 Assets/Scenario/ 下，通过 Addressable group "Demo正式版" 注册（address=Naninovel/Scripts/{name}）。EditorResourceProvider（MasterProvider）持有 pathToGuidMap（237条），包含所有脚本映射。ProjectResourceProvider 从 /Resources/ 路径扫描（文件不在 Resources 下所以不生效）。编辑器下实际加载走 EditorResourceProvider → AssetDatabase.GUIDToAssetPath → LoadAssetAtPath。**Why:** 理解加载链路以便排查"not loaded"错误。**How to apply:** 新增 .nani 脚本后必须：1) 加入 Addressable group（address=Naninovel/Scripts/{name}），2) 运行 Naninovel/Update Metadata，3) Naninovel/Build Resources。
- [2026-07-31 08:33:34] [2026-07-31] 10 个缺失的 .nani 脚本已补加入 Addressable group "Demo正式版"：魔王 1~8、奇遇测试 1、死亡 1。之前只有 Entry/MoWangXian/plot1-5/Title 有 Addressable entry。**Why:** 这些脚本文件存在于 Assets/Scenario/ 但未注册到 Addressable，导致 Use Existing Build 模式下加载失败。**How to apply:** 新增 .nani 文件后检查 Addressable group 是否包含对应 entry。
- [2026-07-31 11:38:58] [2026-07-31] Naninovel 角色"空之魔王"的 Implementation 从 LayeredCharacter 改为 SpriteCharacter。原因：LayeredPrefab 的 layers data 为空（没有子 renderer），导致 LoadAndPlay 预加载角色资源时抛异常中断。改为 SpriteCharacter 后使用已有的 12 张表情 sprite。**Why:** LayeredCharacter 需要 prefab 含子 renderer，但 prefab 配置不完整。**How to apply:** 不要改回 LayeredCharacter，除非修复了 prefab 的 layer renderer 配置。空之魔王可用表情：不满、仰头、仰头哭、哭过后、害羞、开心、得意、惊讶、愤怒、空之魔王 1、鄙夷、空之魔王。不存在"悲伤"表情。
- [2026-07-31 11:39:07] [2026-07-31] 魔王 7.nani 和 魔王 8.nani 之前是空文件，已补写剧情内容。魔王 7 讲述时之魔王退出魔王小队的裂痕事件；魔王 8 讲述空之魔王获得火晶后决定召唤异世界勇者的经过。**Why:** 空文件导致点击对应卡片后 Naninovel 剧本空白。**How to apply:** 如需修改剧情内容，直接编辑 Assets/Scenario/魔王 7.nani 和 魔王 8.nani，编辑后运行 Naninovel/Update Metadata。
- [2026-08-10 20:21:30] [updated 2026-08-10] BattleEventManager 必须挂在 Managers（always active）上，不能挂在遭遇战菜单上（默认 inactive，Awake 不会执行，Instance 永远为 null）。GameManager.Start() 调用 OnBattleStart()，GameOver() 和 LevelEndMenu 胜利路径调用 OnBattleEnd()。遭遇战菜单（Canvas 默认 inactive, scale=0）上挂的是 EncounterManager 组件（非 BattleEventManager），用于干员移动中碰到敌人时弹出"战斗/避让"选择菜单。EncounterManager.cs 曾被误删导致菜单失效，2026-08-10 从 git 历史(12f73db)恢复并重新挂到 BattleScene 遭遇战菜单上，绑定了 Panel/fightButton(战斗)/avoidButton(避让)。OperatorUnit.cs 中的遭遇战逻辑(isEncountering/TryStartEncounter/ResolveEncounter/SetHighlight/OnTriggerEnter2D/Stay2D/Exit2D)同步恢复。IsEvading() 改为 `!chooseToFight && isMoving`（选择战斗时不算避让）。**Why:** 遭遇战菜单 Canvas 默认 inactive，挂在上面的 MonoBehaviour 不会运行 Awake。**How to apply:** BattleEventManager 放在 Managers 上；EncounterManager 放在遭遇战菜单 Canvas 上（它不需要 Awake 运行，由 OperatorUnit.TriggerEncounter 外部调用）。

- [2026-07-31 13:41:53] [2026-07-31] 全部 18 个 LevelConfig 资产的 gridData 已重新设计。之前 Level 11-16 完全空白（全零），Level 3-10 数据过于简单。现在每个关卡都有独特的地图布局：L1=S形路径, L2=双通道十字, L3=螺旋迷宫, L4=之字回廊, L5=中央要塞, L6=双螺旋, L7=分流岛屿, L8=高地战场, L9=狭窄通道, L10=堡垒围攻, L11=河谷, L12=蜂巢, L13=对称竞技场, L14=迷宫回廊, L15=交叉路网, L16=复杂迷宫, L17=Boss宽阔战场, L18=精英对称战场。每关含 1-3 条路径航点。**Why:** 旧关卡场景的地图质量参差不齐，导出的 LevelConfig 资产大量空白。**How to apply:** BattleScene 运行时通过 MapBuilder.BuildFromConfig() 自动从 LevelConfig 生成地图，无需手动维护场景中的 Tilemap。
- [2026-07-31 13:42:03] [2026-07-31] Rest.unity 场景重新设计完成。① 在 RestController GameObject 上添加了 HideNaninovelUIOnLevelLoad 组件，解决从剧情场景进入 Rest 时 Naninovel DontDestroyOnLoad 对象（相机/UI/角色）穿透遮挡问题。② 通过 execute_csharp_script 重建了整个 RestCanvas UI 层级：RestCanvas > Background(完全不透明alpha=1.0)/TopStrip/BottomStrip/Frame(Title/Subtitle/InfoPanel[HpText/GoldText/DrawText/CurseText]/Btn_Rest/Btn_Study/Btn_Forge/Btn_Purify/Btn_Leave/ResultText)/ForgePanel > ForgeFrame > (ForgeTitle/ForgeListRoot/Btn_ForgeClose)。③ RestController.cs 已适配新层级：ForgePanel 和 ForgeListRoot 通过 _canvas.transform.Find 递归查找（非 GameObject.Find），Btn_ForgeClose 通过 ForgeFrame 查找。**Why:** 旧 UI 背景半透明(0.92)导致 Naninovel 透出，按钮简陋且 ForgePanel 因 inactive 无法绑定。**How to apply:** 修改 Rest 场景 UI 时注意 ForgePanel 现在是 ForgePanel>ForgeFrame>子对象 的三层结构。
- [2026-07-31 13:42:08] [2026-07-31] RestController.OnRestAmbush() 已改用新架构加载战斗：先尝试加载 restLevel 的 LevelConfig（Resources/LevelConfigs/Level_{N}_Battle），不可玩则回退到 Level_01_Battle，通过 LevelSceneLoadContext.SetLevelConfig() + VideoSceneLoader.LoadScene(SceneNames.BattleScene) 加载。不再直接 LoadScene("level N")（level 12-16 不在 Build Settings 会崩溃）。**Why:** 旧代码直接加载 "level N" 场景，但很多关卡是空壳或不在 Build Settings 中。**How to apply:** 事件场景中需要跳转战斗时，用 LevelConfig + BattleScene 架构，不要直接加载关卡场景文件。
- [2026-07-31 13:42:15] [2026-07-31] 事件场景（Rest/RandomEvent_*/GoldShop）都没有 HideNaninovelUIOnLevelLoad 组件，导致从 plot 场景（Naninovel 激活状态）进入这些场景时 Naninovel 的 DontDestroyOnLoad 对象（相机/UI/角色）穿透并遮挡场景。Rest 场景已修复（在 RestController 上添加了 HideNaninovelUIOnLevelLoad）。RandomEvent 和 GoldShop 场景如有类似问题，需同样添加该组件。**Why:** Naninovel 的对象在 DontDestroyOnLoad 中不会被场景切换自动销毁。**How to apply:** 所有非 Naninovel 的事件/商店场景都应在入口控制器上挂 HideNaninovelUIOnLevelLoad。
- [2026-07-31 14:09:04] [2026-07-31] 敌人移动系统是纯航点寻路（Enemy2.cs: Vector3.MoveTowards 逐段走向下一个航点），没有碰撞检测，不会自动避墙。如果两个航点之间的直线穿过墙壁/空白格子，敌人会穿墙。LevelConfig.gridData 中墙壁(2)和高台(3)格子不阻挡敌人移动——只有航点连线决定路线。
- [2026-07-31 14:09:04] [2026-07-31] BattleScene 和 Level 1 场景的三个 Tilemap sortingOrder 已修复：Ground=0（底）、HighGround=1（中）、Wall=2（顶）。之前全是 0 导致高台瓦片被同位置的地面瓦片盖住。HighGroundTile 颜色从 (0.65,0.82,1.0) 改为 (0.35,0.55,0.95) 使蓝色更醒目；WallTile 从 (0.3,0.3,0.38) 改为 (0.25,0.2,0.35) 加深紫色。
- [2026-07-31 19:23:40] [2026-07-31] L11-L17+L19-L22 的 gridData 和路径已全部重做：路径严格水平/垂直（无斜线），全部在地面格上（不穿墙/高台），终点统一在守护点 grid(13,9)=world(3.5,3.5)。守护点只有一个，是所有路线终点，敌人到达此处扣守护点血。用 execute_csharp_script 内联 for 循环铺地面+验证，不用 local function（Roslyn 脚本中 local function 有作用域问题）。

- [2026-07-31 14:39:04] [2026-07-31] BattleScene 的 MapBuilder 瓦片引用已从纯色 tile_0000 换成有纹理的瓦片：groundTile=Ground_Grass（Grass0.png，128x128 草地噪点纹理），wallTile=Wall_Stone（Stone0.png，灰色裂纹石头），highGroundTile=HighGround_Stone1（Stone1.png，无缝石板路）。这些瓦片资产在 Assets/map/替身/ 目录下。之前三种地形全用同一个 16x16 纯色方块靠 m_Color tint 区分，非常单调。
- [2026-07-31 18:42:35] [2026-07-31] 新增4个战斗关卡 LevelConfig 资产：L19迷雾沼泽(森林/S形4层走廊单路径)、L20双峰峡谷(沙漠/上下双路径)、L21螺旋深渊(Boss/外圈到中心螺旋路径/Bigboss)、L22十字要塞(精英/十字交叉双路径)。全部用 execute_csharp_script 通过 AssetDatabase.CreateAsset 创建，路径不穿墙已验证。BattleSceneBootstrap.useDefaultConfigForTesting 可指定 defaultTestConfig 直接测试关卡。
- [2026-07-31 19:23:45] [2026-07-31] 关卡地图可视化编辑器已创建：Assets/Editor/LevelConfigMapEditor.cs，菜单 Tools→关卡地图编辑器。三种模式：View(只读)、Paint(左键刷地形/右键循环切换)、Path(左键添加航点/右键删除/拖动移动航点，自动吸附网格+打通地面)。支持 Ctrl+Z 撤销和 Ctrl+Y 重做（通过 Undo.RegisterCompleteObjectUndo）。路线终点标记为蓝色"守"=守护点，起点标记为绿色"出"=刷怪点。Path模式下可高亮显示路径经过的格子。
- [2026-08-01 06:57:14] [2026-08-01] 固定模式跳转链路验证通过。30个 LevelConfig.asset 已全部导出（L1-L30），Editor 模式下测试：点击按钮2 → Resources.Load LevelConfig → BattleSceneBootstrap 初始化关卡成功 (ID=1) → LevelRunModifiers HP×1.00 SPD×1.00 固定模式无修饰 → 敌人池正常初始化。RunModifierConfig.asset 已创建。**Why:** 设计文档标记"尚未导出"是旧信息，实际已全部就绪。**How to apply:** 固定模式可直接测试，无需额外操作。
- [2026-08-01 07:18:04] [2026-08-01] 开局 UI 模式选择改为纯场景对象（不再代码自动创建）。RogueEntryController.EnsureGameModeUI() 只做绑定和初始化，不创建 UI。场景中已有 GameModeLabel + GameModeDropdown 两个可视化对象，可在 Hierarchy 中自由调整。编辑器脚本 Tools/创建关卡模式下拉 可一键重建。样式：深蓝半透明背景 + 金色 Outline 边框 + 金色 ▼ 箭头 + 下拉列表悬停高亮。**Why:** 用户要求可视化可调，不要代码创建。**How to apply:** 直接在 Hierarchy 选 GameModeDropdown 调整位置/大小/颜色；如需重建，删掉旧对象后点 Tools/创建关卡模式下拉。

- [2026-08-01 06:57:14] [2026-08-01] Level 7 (Level_07_Battle.asset) 的 waveGroups=0，导致 IsPlayableLevelConfig() 返回 false，触发回退到可用战斗关卡。这是唯一一个波次数据为空的 LevelConfig。**Why:** 创建时可能遗漏了波次配置。**How to apply:** 如需修复，用关卡地图编辑器或 LevelConfigMapEditor 给 L7 补上 waveGroups 数据。
- [2026-08-01 07:33:59] Git global proxy was dead (http://127.0.0.1:7897, likely Clash), causing all pushes to fail. Removed with `git config --global --unset http.proxy`. auto-git-push.ps1 v3 now auto-detects and removes non-working proxies before push.
- [2026-08-02 06:51:43] [updated 2026-08-02] 142MB TMP SDF font atlas file `SIMKAI SDF(1).asset` blocks GitHub push (100MB limit). .gitignore pattern `Assets/Fonts/*SDF*.asset` was too narrow — the file ALSO exists at `Assets/先存放文件夹/Fonts/` and got re-committed there on 2026-08-01 21:45, causing push failure. **Fix needed:** use `**/*SDF*.asset` in .gitignore to cover all paths, AND `git rm --cached` the file from history. SDF atlases can be regenerated locally from .ttf font files.

- [2026-08-02 06:51:47] [updated 2026-08-02] Auto-push infrastructure fully working: `auto-git-push.ps1` v4 (push every 6h via Task Scheduler), `auto-git-bundle.ps1` (daily bundle at 22:00 to `D:\GitBackups\`). Three fixes applied after initial setup: ① `git config --global core.sshCommand "C:/Windows/System32/OpenSSH/ssh.exe"` — Git's bundled msys2 SSH can't handle Chinese username path `ZhuanZ（无密码）`, uses wrong encoding → Host key verification failed. ② Task Scheduler battery settings: DisallowStartIfOnBatteries=false, StopIfGoingOnBatteries=false, StartWhenAvailable=true (missed runs auto-catch up on next boot). ③ `-WindowStyle Hidden` added to task Arguments so no terminal window pops up. First push (1.29 GiB) completed via SSH. Subsequent auto-pushes confirmed working (08-01 14:46 Push OK, 08-01 21:45 Push FAILED due to SDF file re-commit). Script is `git push origin main` (follows remote SSH URL), no HTTPS proxy logic.

- [2026-08-01 08:47:24] [project] Git repo pack is 1.29 GiB (all legitimate PNG/JPG art assets in Assets/Backgrounds/, Assets/Characters2/, Assets/杂和卡/ — no Library/Temp/Build garbage). `git gc --aggressive` does NOT shrink it because PNG/JPG are already compressed. First push to GitHub keeps failing with `Connection reset` due to data volume + VPN instability. HTTP/1.1 vs HTTP/2 makes no difference. Once first push succeeds, subsequent pushes are incremental (few MB). **Why:** 3000+ image files committed in one batch. **How to apply:** don't expect gc to fix large binary repos; the only fix is a stable long-enough connection for the first push.
- [2026-08-01 18:41:27] [2026-08-01] GameMode 随机/混合模式重新设计：旧设计只修改战斗数值（敌人血量/速度倍率），不改关卡顺序——这是设计错误。新设计：随机模式打乱关卡顺序（每局用 RunSeed 派生 RNG 洗牌），混合模式前 cutoff 关固定后续打乱，固定模式原样。ShouldApplyModifiers() 已改为始终返回 false，ApplyRunModifiers 代码路径已废弃但仍保留在 LevelConfig.cs 中。**Why:** 用户指出随机关卡应该改变关卡顺序而非改数值。**How to apply:** GetLevelConfigIdForStage(stageNumber, levelType) 是核心入口，LevelNodeButton.TryLoadLevelConfig 调用时传入 GetLevelType(_levelNumber)，按类型从对应池取 ID。
- [2026-08-01 18:41:30] [2026-08-01] 随机关卡按类型分池：普通关卡只随机到 levelType=NormalBattle 的 LevelConfig，精英只随机到 Elite，Boss 只随机到 Boss。商店/休息/事件类型不参与随机。GenerateShuffledOrder() 扫描 Level_01~Level_50 的 .asset，按 config.levelType 分成三个池各自独立洗牌。**Why:** 用户要求不同类型关卡不能混随机。**How to apply:** _typeShuffledPools 是 Dictionary<LevelType, int[]>，GetShuffledLevelIdForType 按 (stageNumber-1) % pool.Length 取。
- [2026-08-01 19:32:37] [project] 纯剧情奇遇示例「残影之泉」已创建。设计模式：RandomEventData 选项配 naniScriptName+naniLabel → 跳 Naninovel 播剧本 → .nani 内用自定义命令 @setFlag 设置 AdventureFlag → 返回事件场景显示结果。SetFlagCommand.cs 在 Assets/Naninovel脚本/ 下，继承 Naninovel.Command，[CommandAlias("setFlag")]，调用 StoryCardUnlockState.SetAdventureFlag()。.nani 脚本在 Assets/Scenario/残影之泉.nani，两个 label（gaze/ask）对应不同选择分支，末尾用 @stop 防串播。两张 StoryCardData（act1_spring_01「初冠」/act1_spring_02「轮回」）在 Resources/StoryCards/，unlockConditionType=AdventureChoice。**How to apply:** 设计新的纯剧情奇遇时复制此模式：1) .nani 用 label 分支+@setFlag+@stop，2) RandomEventData 选项的 naniScriptName/naniLabel 指向对应 label，3) StoryCardData 用 AdventureChoice 条件+对应 flag 名。
- [2026-08-01 19:34:23] [2026-08-01] Plot场景从16关改为21关（15普通+5精英+1Boss）。21个按钮就是地图上的全部节点，商店/奇遇/休息不占额外按钮位置。SimpleLevelRandomConfig新增第5组group17to21（3精英+1Boss），Boss从原group13to16移走。LevelRandomizer初始化上限16→21。7个主线碎片StoryCardData已创建（act1_main_01~07，碎片名：启程/疑云/追迹/暗涌/转折/迫近/终局），plot1.nani已加6个占位label。**Why:** 用户要求21关设计，穿插节点不算按钮。**How to apply:** 按钮位置=战斗序号（1-21），不需要GetBattleLevelIndex转换。
- [2026-08-01 19:35:11] [project] 大局(Act)系统已创建。核心架构：ActConfig(ScriptableObject) 定义大局数据（actId/关卡池/Boss/Naninovel脚本/节点数），ActRegistry 从 Resources/ActConfigs/ 加载所有大局配置并管理解锁/完成状态。RogueRuntimeState 新增 CurrentActId/CurrentActConfig/StartAct()/CompleteCurrentAct()。GetLevelConfigIdForStage 优先从 ActConfig 关卡池取 ID。LevelMapController.Awake 从 ActConfig.totalNodes 动态生成 levelOrder。LevelSceneLoadContext 传递 actId。无 ActConfig 时全部回退旧逻辑。第一个 ActConfig_1.asset 已创建（"魔王的试炼", 21节点, Boss=L16, plot1.nani）。
- [2026-08-01 21:52:57] [project] RandomEventController 恢复了进场自动播放 Naninovel 的功能（原来被移除）。通过 RandomEventData.naniEntryScriptName 字段触发：DisplayEvent() 检测到该字段非空时，直接 JumpToNaninovel() 跳 Title 场景播放剧本，播完后 s_entryNaniReturn=true 直接 ReturnToMap() 回地图。naniEntryScriptName 虽然标记了 [Obsolete] 但现在重新启用了。纯剧情奇遇用 .nani 内的 @choice 命令做选择（不再用 RandomEventController 的按钮UI），选择后用 @goto 跳到对应 label，末尾 @stop 结束。
- [2026-08-03 10:25:59] [updated 2026-08-03] ActConfig.fixedNodeTypes 和 FixedNodeType 类已删除。所有节点类型现在完全由 StSMapGenerator.AssignNodeTypes() 按楼层深度加权随机分配（Floor 1 必为普通战斗，早期不出精英，Boss 前有强制休息等）。LevelRandomizer.SetActConfig() 也已删除。LevelNodeButton 的 Shop/RandomEvent/Rest 节点进入时自动 MarkCompleted 解锁下一关，可反复进入。

- [2026-08-02 06:41:28] [2026-08-02] plot.unity 摄像机位置漂移到 (-22.32, 25.76) 导致 Canvas(ScreenSpaceCamera) 整体偏移，已重置到 (0,0,-10)。LevelConnectionConfig.asset 原来只有 1→16 的 18 条连线，按钮 17-21 之间无连线，已补充 16→17→18→19→20→21。Canvas 配置：renderMode=ScreenSpaceCamera, planeDistance=100, CanvasScaler=ConstantPixelSize(800x600), Camera orthoSize=5。ScrollRect Content 高度 7200，按钮从 anchoredPos y=-6914(按钮1) 到 y=-350(按钮21) 垂直排列。
- [2026-08-02 06:41:30] [2026-08-02] 10个 ActConfig 资产已创建（ActConfig_1~10），放在 Assets/Resources/ActConfigs/。关卡池采用轮转分配，50个 LevelConfig 在 10 个大局间循环复用。Boss 轮流用 L16/L21/L43。Act 1(魔王的试炼/plot1)默认解锁，Act 2(火之魔王/plot2)~Act 5(王国秘辛/plot5) 有 Naninovel 脚本，Act 6-10 暂无脚本待设计。Act 2-10 解锁条件为通关前一个大局。ActRegistry 从 Resources/ActConfigs/ 加载全部配置。
- [2026-08-02 06:59:46] [2026-08-01] 碎裂之镜系统已完全删除。StoryMirrorPanel.cs、StorySetData.cs、FragmentShard.cs、FragmentShard.prefab 全部删除，场景中 StoryMirrorPanelRoot 对象也删除。StoryCardPanel.cs 中的 setDatabase 字段和 SetSetFilter 方法已清理。剧情碎片收藏只保留主卡片网格（StoryCardPanel），它已有翻页、分类筛选、状态筛选、锁定显示功能。设计文档剧情与游戏融合设计.md 中碎裂之镜和套系相关内容已删除。StoryCardData.cs 中的 fragmentSetId/isKeyFragment 字段保留但不再被使用（向后兼容）。
- [2026-08-02 07:10:58] [updated 2026-08-02] 奇遇场景统一为1个 RandomEvent.unity，5个旧场景（Cave/Forest/Grave/Street/Temple）已删除，SceneNames.cs 中对应常量也清理为 RandomEvent="RandomEvent"。奇遇分两种模式：①纯Naninovel剧情奇遇（naniEntryScriptName非空）→ 进场0.3s后跳Title播剧本，@choice goto:.label 分支选择，播完直接回Plot；②普通奇遇（naniEntryScriptName为空）→ 显示UI按钮，选选项后执行outcomes回Plot。现有31个事件（20旧+1残影之泉+10新纯剧情）。10个新纯剧情奇遇：断剑之冢/镜中幻影/夜话魔森/旧日书信/星陨之地/渡魂之河/封印之地/月下独白/深渊低语/迷途之歌，全部 oneShot=true、playNaniOnEnter=true，每个3个@choice分支+@setFlag。注意：Naninovel .nani 中不能使用带空格的表情名（如"空之魔王 1"），必须用"空之魔王"。
- [2026-08-02 07:20:28] [2026-08-02] 剧情卡片系统已大幅简化：删除了所有分类/筛选/套系系统。StoryCardCategory 枚举、StoryCardStatusFilter 枚举、category/fragmentSetId/setIndex/isKeyFragment 字段、SetComplete/FragmentChain/FragmentViewed 解锁条件类型全部删除。StoryCardPanel 只保留翻页+锁定/解锁显示，不再有分类筛选和状态筛选。场景中 CategoryFilterGroup 和 StatusFilterGroup 已删除。设计文档已同步更新。
- [2026-08-02 07:20:33] [2026-08-02] StoryCardCollection 场景有三个面板曾同时存在导致冲突：StoryCardPanelRoot（翻页卡片网格）、CollectionController（旧的无翻页列表）、StoryMirrorPanelRoot（碎裂之镜，已删除）。修复后：StoryCardPanelRoot 激活、CollectionController 守卫改用 FindObjectsByType<StoryCardPanel>(FindObjectsInactive.Include) 检测。CardGridRoot 下预放的手动按钮全删，由代码按 cardsPerPage=8 自动创建。
- [2026-08-02 08:49:05] [2026-08-02] PreBattleDialogue 系统重新设计：不再全部跳转 Naninovel。PreBattleDialogue 新增 useNaninovel(bool，默认false) 和 inSceneLines(string[]) 字段。useNaninovel=false 时走场景内文字对话（NewbieTutorialController），对话内容通过 LevelSceneLoadContext.SetPreBattleDialogueLines() 跨场景传递给 BattleScene。NewbieTutorialController.Start() 改为从 LevelSceneLoadContext 读取对话内容，无内容则跳过不显示。ActConfig_1.asset 已配置6个战前对话（stages 1/5/10/15/20/21）全部 useNaninovel=false。要给某关加 Naninovel 对话：在 ActConfig_1.asset Inspector 中勾 Use Naninovel + 填 Label Name。
- [2026-08-02 09:06:51] [2026-08-02] [project] GUID断裂根因：commit 12f73db (2026-07-04) 把 Assets/Backgrounds/, Assets/杂和卡/, Assets/Characters2/ 加入 .gitignore 并删除了 .meta 文件。commit f3c9986 重新纳入时只提交了部分 .meta（hex GUID）。后来文件移动到 Assets/先存放文件夹/ 后，Tuanjie 引擎为部分文件生成了新的 Base64 GUID，导致场景/预制体中的旧 hex GUID 引用断裂。已从 git 历史恢复 109/145 个断裂 GUID。剩余 ~36 个 .meta 文件从未被提交，旧 GUID 永久丢失，需在 Unity 编辑器中手动重新拖拽 sprite。**Why:** .meta 文件曾被视为可忽略文件，导致 GUID 断裂。**How to apply:** 永远不要把 .meta 文件从 git 中删除或加入 .gitignore；移动资产后需保持 .meta 文件不变。
- [2026-08-02 21:57:08] [2026-08-02] StS分叉路径地图系统已创建。核心文件：MapGraph.cs（图数据结构）、StSMapGenerator.cs（地图生成器，种子化路径分叉+合并+楼层规则约束）、LevelMapController.cs（重写为动态生成节点按钮+连线）。LevelNodeButton.cs 支持 MapNodeData 图节点模式。LevelProgress.cs 新增节点完成追踪（MarkNodeCompleted/IsNodeUnlocked）。RogueRuntimeState 新增 CurrentMapGraph + SetMapGraph。ActConfig 新增 mapFloorCount/mapMaxColumns/mapPathCount。LevelType 枚举新增 Start。**关键bug**：LevelLineConnector（[ExecuteAlways]）在 OnEnable 中会清除所有 Lines 子对象并重建旧连线，必须在 BuildMapUI 中禁用它。
- [2026-08-03 10:26:14] [updated 2026-08-03] 大局2（ActConfig_2 "火之魔王"）已完善：① plot场景默认大局从1改为2（LevelMapController.Awake + RogueRuntimeState.ForceResetRun 都改为 GetActConfig(2) ?? GetFirstAct()）；② Level_43 levelType 从 NormalBattle 改为 Boss，displayName 改为"火之魔王"；③ ActConfig_2 配置了 preBattleDialogues（5个：节点1/5/10/15/21，全部 useNaninovel=false 场景内文字）、storyFragments（7张）；fixedNodeTypes 已删除，不再使用；④ 创建7张 StoryCardData（act2_main_01~07，scriptName=死之魔王，分别对应潜入/密文/日记/叛变/六十二年/生命同步/读心术 label，前5张 LevelClear 解锁1/5/10/15/20关，后2张 BossDefeated 解锁）；⑤ 创建2个 RandomEventData（evt_death_king_palace 纯剧情奇遇 playNaniOnEnter=true + evt_death_king_rivals 混合奇遇3选项）；⑥ 创建 Assets/Scenario/死之魔王.nani（7个label碎片化叙事）并注册到 Naninovel Assets.json。**Why:** 用户要求把死之魔王.md文本文档转化为游戏内资产并完善大局2配置。**How to apply:** 死之魔王.nani 需运行 Naninovel→Update Metadata 才能加载；evt_death_king_palace 使用了 playNaniOnEnter=true。

- [2026-08-02 22:06:28] [2026-08-02] ActConfig 关卡池数据验证：Level_23~43（大局2普通+精英+Boss池）全部有有效数据（gridData 非空、waveGroups≥3）。但 Level_38~42（精英池）的 levelType 全部是 NormalBattle 而非 Elite——这些关卡虽然被放入精英池但 levelType 字段未改。LevelRandomizer 按类型分池随机时用的是 ActConfig.eliteLevelPool 中的 ID 而非 LevelConfig.levelType 字段，所以不影响随机逻辑，但 Inspector 中显示不正确。如需修复可批量改 Level_38~42 的 levelType 为 Elite。
- [2026-08-03 10:26:30] [updated 2026-08-03] StS分叉路径地图布局参数（plot.unity 场景）：ScrollView offsetMin=(620,0) offsetMax=(-20,0) 避开左侧 HeldCardsFrame；Viewport offset 归零填满 ScrollView；Content pivot=(0.5,1.0) sizeDelta=(0,7240)；MapBackground anchor=(0.5,1.0) pivot=(0.5,0.5) pos=(0,-3620) size=(1280,7240) 匹配 Content 宽高；节点和线的 RectTransform anchor 必须设为 (0.5,1.0)+pivot(0.5,0.5) 才能和 Content pivot 对齐。floorSpacing=440 columnSpacing=280 buttonSize=(120,120)。菜单 Tools→生成StS分叉地图 可在编辑模式下重新生成（固定种子12345）。

- [2026-08-03 10:11:41] [2026-08-03] LevelLineConnector 已适配 StS 分叉路径地图：GenerateLines() 优先从 RogueRuntimeState.CurrentMapGraph 读取节点连线关系，按 Node_{id}_{type} 命名查找 RectTransform。GetGradientColor 按节点 ID 比例计算渐变色（蓝→紫→金）。LevelMapController 不再禁用 LevelLineConnector，创建节点后调用 GenerateLines()。LevelLineConnector 配置：lineWidth=4 glowWidth=14 glowAlpha=0.12 showArrows=true enableFlowAnimation=true flowSpeed=80。流动光点仅在运行时生成（Application.isPlaying 检查）。
- [2026-08-03 18:36:28] [2026-08-03] 干员玩法设计文档：Assets/游戏设计文档/03-干员玩法设计.md。核心设计：① 29名干员分1~5星(maxStarRating)，所有干员初始都是1星(currentStar=1)，可用金币强化到上限；② 1星时高星上限干员已比低星上限强（baseStatMultiplier按maxStarRating递增）；③ 开局在RogueEntry场景自选干员，星数预算7（选中干员currentStar之和≤7）；④ 天赋卡=遗物（战后3选1不混入干员），干员=角色（开局自选+剧情获取为主）；⑤ 跨大局继承：选5名带入下一个大局，其余因剧情离队，干员星级保留、天赋卡清空；⑥ 部署采用方案B单实例：撤退返还50%DP+正常冷却，阵亡不返还+冷却×1.5；⑦ 满星(maxStarRating)解锁职业被动；⑧ 干员解锁状态永久保留（PlayerPrefs）。

- [2026-08-03 18:23:10] [2026-08-03] 干员头像sprite已全部赋值：29个OperatorData.asset的icon字段已从共用占位图改为各自独立的头像sprite，保存在Assets/Sprites/OperatorPortraits/。来源：13个从Q版动画帧(Assets/先存放文件夹/animation/勇者/)，7个从Characters2角色立绘，9个从暂时用不到/role/角色图池。AI生成(huoshan_seedream/frontier-game-design)均返回401无权限，改用已有素材分配。
- [2026-08-03 18:23:17] [2026-08-03] BattleScene干员卡片修复三个渲染问题：①角色画布下11个干员卡片的localScale.z=0导致Image在3D空间退化不可见，改为1；②角色画布Canvas sortingOrder=0被角色商店处SpriteRenderer(sortingOrder=3)遮挡，改为10；③珑/晶的localPosition.z=-90导致脱离Canvas平面，改为0。卡片缩放从0.014→0.018增大可见性，5张活跃卡片Y坐标重排为3.5/1.5/-0.5/-2.5/-4.5。
- [2026-08-03 20:18:37] [2026-08-03] RogueEntry 场景干员选择面板已创建：OperatorSelectionPanel.cs 挂在 UIRoot 下的 OperatorSelectionPanel 对象上。UI 对象静态创建在场景中（编辑器可见不运行也能看到），脚本运行时只负责数据绑定和交互。三区域布局：顶部星数预算(7)、中间立绘预览(台词+星级+属性)、右侧干员列表(分已解锁/未解锁两段可滚动)。29个 OperatorData 资产已配置 maxStarRating(1~5) 和 isInitialAvailable(6名初始可用: 斥候/战术/连弩/净化/突击/游击)。编辑器菜单 Tools→干员选择面板→重建面板/设置干员星级。Canvas 使用 ScreenSpaceCamera + ConstantPixelSize，scale=0.107，坐标空间 1920×1080，UI元素需用大字号(22~36px)和大尺寸才能在屏幕上可见。
- [2026-08-05 07:12:52] [2026-08-03] 角色素材规格标准：①Q版战斗sprite：1024×1024 PNG透明背景，放 Assets/Sprites/OperatorPortraits/（干员）或敌人对应目录，文件大小目标200-500KB；②剧情对话立绘：2048×2048 PNG透明背景，放 Assets/NaninovelData/Resources/Naninovel/Characters/角色名/，文件大小目标1-2MB；③表情差分：2048×2048，每角色3-5张，约1.5MB/张。即梦生成参数：Q版用1024²不勾2K批量2，立绘用2048²勾2K批量1。三角色（死之魔王/弑神者/神猎者）合计约28MB原始/8-10MB压缩后。
- [2026-08-05 13:57:08] [2026-08-03] 角色生成提示词大全文件：Assets/游戏设计文档/角色生成提示词大全.md，含50个角色提示词（王室/战士/法师/宗教/弓箭手/刺客/商人/魔物/植物/特殊职业）。每条内嵌画风关键词（明日方舟风格+软cel shading+masterpiece）+全身立绘要求+参考图声明。负面提示词在文件顶部统一列出。画风统一方案：用即梦图生图模式上传空之魔王立绘做参考图（参考强度70-80%），提示词只写角色描述不写画风。豆包下载图片在 C:\Users\ZhuanZ（无密码）\Documents\Downloads\。
- [2026-08-05 18:30:52] 空之魔王（孔心）的哥哥是孔雷——在地球继承了时之魔王权能并成为勇者，打破时空回来找妹妹。孔雷设定为人类，正常耳朵（无角无精灵耳）。Characters2/时之勇者/ 下的图片不是孔雷，是旧设计角色与孔雷暂时重名，用户已弃用。用户正重新设计孔雷形象：银白短发、淡紫瞳孔（与妹妹呼应）、黑银哥特风、腰间佩剑鞘。
- [2026-08-05 19:36:37] [updated 2026-08-05] 角色生成提示词大全文件：Assets/游戏设计文档/角色生成提示词大全.md。当前共195条提示词（#1-15干员，#16-25普通NPC，#26-50剧情角色+王室护卫+服务NPC，#51-75非人型怪物，#76-115第一批NPC，#116-155第二批NPC，#156-175美女角色含战斗/贵族/奇幻种族/平民NPC各5个，#176-185萝莉少女角色10个，#186-195成熟女性角色10个）。每条提示词是完整代码块，包含画风前缀+角色描述+负面提示词，直接复制粘贴到即梦即可使用。即梦参数：2048×2048勾2K批量1（立绘）或1:1标清批量2（Q版）。画风前缀已强化全身约束（"必须画完整全身立绘从头顶到脚底完整可见"），负面提示词已加中英双语防裁切词。注意：提示词中写的"白色背景"实际生成时应忽略，一律用透明背景（is_segmentation=true）。#176-195新增角色弱化了职业属性，侧重外貌和性格描述，方便用户后续灵活分配身份。
- [2026-08-06 14:30:01] [2026-08-06] 测试阶段关卡通关后可重复进入：RogueRuntimeState.ForceResetRun() 和 StartRunIfNeeded() 中的 LevelProgress.ClearNodeProgress() 在编辑器测试模式下（IsTestUnlockEnabled()==true）不再执行，保留已通关节点记录。正式发布时正常清除。**Why:** 用户测试时通关后重开新局，节点进度被清空导致已通关关卡无法再进。**How to apply:** 仅影响测试流程，发布版本行为不变。
- [2026-08-07 15:37:03] [项目] 本机网络（国内/GFW）直连 GitHub 下载 release 大文件会被 connection-reset（Connection reset 10054），必须用镜像。rembg 模型下载脚本（E:\走路\download_model.py）用断点续传+RANGE头，镜像 gh-proxy.com（前缀 https://gh-proxy.com/https://github.com/...）实测可用，约1.5MB/s，ghfast.top/mirror.ghproxy.com/ghproxy.net 不稳定。**How to apply:** 以后从 GitHub 下载模型/大文件直接用 gh-proxy.com 镜像+续传脚本，别直连。另注意 rembg2.0.78 的 new_session() 模型名用连字符（如 birefnet-portrait → "birefnet-portrait"），不是模块名下划线；且 BiRefNet 各模型都是927.6MB左右，并非网上说的180MB。
- [2026-08-07 21:30:11] [2026-08-07] 巨蟹角色（石头蟹怪物）的走路动画用 generate_sprite_sequence AI 生成成功，21帧 frontRun 类型。但之前多次视频生成都效果不好，说明非人形角色（螃蟹）的走路动画 AI 序列帧生成比人形角色更可靠。AI 生成结果在 Assets/TJGenerators/History/Sequence_20260807_212919/，已绑定到 plot.unity 场景的 1 (11) 对象。
- [2026-08-10 13:33:17] [2026-08-10] 魔族角色正式接入战斗系统第一阶段完成。13个角色（傀儡师/巨蟹/时之魔王/星豹/死亡剑士/死囚/火焰战士/熔岩虫/狂兽战士/空之魔王/紫雷狼/藤曼战士/雪女）的基础设施已就绪：①AnimatorController全部重建为 IsFight(bool) 参数 + Walk/Attack 双状态 + 双向转换（EnemyFightAnimator 和 OperatorAttackAnimator 都用 IsFight 驱动切换）；②基础预制体在 Assets/Resources/人物/魔族/，组件=SpriteRenderer+Animator+BoxCollider2D(trigger)+Rigidbody2D(kinematic)+HealtherCanvas(HP/蓝条)；③头像Sprite在 Assets/Sprites/CharacterPortraits/魔族/。**Why:** 用户说"之前的战斗系统基本都是瞎搞没有正式制作"，这是正式制作阶段。用户明确要求不分类、不定职业/技能，后续自己划分。**How to apply:** 后续给角色加组件时（Enemy2/OperatorUnit/职业组件/数据资产），在这些基础预制体上叠加，不要重建。魔物法师文件夹不存在（只有13个角色不是14个）。
- [2026-08-10 18:48:10] [2026-08-10] 右侧干员商店已改为可滚动竖条面板（整卡吸附模式）。面板结构：角色画布(WorldSpace Canvas, sortingOrder=10) > 干员商店面板(x=7.8, sizeDelta=2.4×9.6) > 背景(Image半透明深色) + Viewport(RectMask2D裁剪) > Content。卡片尺寸1.2×1.2，间距0.15，13张魔族卡片纵向排列。OperatorShopScroll.cs 使用整卡吸附：滚轮每次移动一个卡片，拖拽松手后吸附到最近整卡位置(snapLerpSpeed=12)。13个魔族干员卡片头像已从各自角色文件夹(Assets/new characters/魔族/{角色名}/)中用户指定的即梦图片赋值。费用文字fontSize=2，名字fontSize=3。旧"角色商店处"SpriteRenderer已删除。

- [2026-08-10 18:13:24] [2026-08-10] 13个魔族干员OperatorData资产已创建在 Assets/数据2/干员数据/，预制体在 Assets/Resources/人物/魔族/，头像在 Assets/Sprites/CharacterPortraits/魔族/。干员数值和职业定位由用户指定：傀儡师=近卫(单打独斗)、巨蟹=重装(坦克抗伤)、时之魔王=特种(暂停全图敌人)、星豹=先锋、死亡剑士=先锋、死囚=重装(扛伤加控制)、火焰战士=近卫(单打独斗)、熔岩虫=特种(吞噬<40%血非精英敌人,有消化冷却)、狂兽战士=近卫(战士干员)、空之魔王=狙击(地面+高台远程射手,canStandOnGroundAndHighGround=true)、紫雷狼=先锋、藤曼战士=重装(坦克扛伤)、雪女=术师(法师)。两个自定义技能：Skill_TimeStop(时之魔王,autoActivate,StunAllEnemies)、Skill_Devour(熔岩虫,autoActivate,吞噬阻挡中血量≤40%的非精英敌人)。其余11个角色用OperatorUnit.TryAttachDefaultSkill()按opType自动分配默认技能。BattleScene中13张卡片operatorData已全部赋值。
- [2026-08-10 18:55:46] [2026-08-10] 魔族干员 Animator 控制改为方案B：预制体 Animator.enabled=false（13个魔族预制体已批量设置），运行时由 OperatorUnit 控制——MoveRoutine 开始时 EnableAnimator()、结束时 DisableAnimator()+恢复 originalSprite；OperatorAttackAnimator.SetFight() 进入战斗时 EnableAnimator()、退出战斗且不在移动中时 DisableAnimator()。OperatorUnit.Start() 缓存 animator 和 originalSprite。**Why:** AnimatorController 默认状态是 Walk，实例化后立即覆盖 SpriteRenderer 的预制体默认 sprite，导致购买角色显示走路第一帧而非预制体默认图；到达目的地后走路动画不停止。**How to apply:** 后续新增角色预制体时 Animator.enabled 必须设为 false；OperatorUnit.cs 的 EnableAnimator/DisableAnimator 方法供其他组件调用。
- [2026-08-10 18:55:46] [2026-08-10] BattleScene 干员商店面板最终布局（角色画布 WorldSpace Canvas）：面板 sizeDelta=2.4×9.6 at x=7.8；标题栏"干员招募"在顶部(height=0.4, fontSize=0.3, 金色)；Viewport offsetMin=(0.1,0.1) offsetMax=(-0.1,-0.45) 留出标题空间；卡片尺寸 2.0×2.2，间距0.15，13张纵向排列。每张卡片结构：根 Image(深蓝半透明0.10,0.11,0.20,0.95) > 立绘(anchor 0.04-0.96 × 0.22-0.96, preserveAspect) + 分隔线(0.06-0.94 × 0.18-0.20, 金棕色) + 名字(0.06-0.65 × 0.02-0.17, fontSize=0.2, 白色左对齐) + 费用文字(0.65-0.96 × 0.02-0.17, fontSize=0.25, 金色右对齐)。OperatorShopScroll cardHeight=2.2 cardSpacing=0.15。**How to apply:** 修改商店面板布局时参照以上参数；字体大小必须用世界单位小数。
- [2026-08-10 19:29:27] [2026-08-10] 魔族干员预制体组件补齐完成（第二阶段）。13个预制体在 Assets/Resources/人物/魔族/ 的组件配置：①先锋(星豹/死亡剑士/紫雷狼)：blockCount=0 + VanguardRecon + SpawnerContactAttacker；②近卫(傀儡师/火焰战士/狂兽战士)：blockCount=2/2/3 + GuardTrait + SpawnerContactAttacker；③重装(巨蟹/死囚/藤曼战士)：blockCount=3/3/4 + SpawnerContactAttacker（重装maxBlockCount无固定上限，按个体配置）；④术师(雪女)：IgnoreDefenseAttacker(常驻破甲) + RangedAttacker + AttackRangeVisualizer+LineRenderer；⑤狙击(空之魔王)：canStandOnGroundAndHighGround=true + RangedAttacker + AttackRangeVisualizer+LineRenderer + baseCritChance=25；⑥特种(时之魔王/熔岩虫)：自定义Skill_TimeStop/Skill_Devour + SpawnerContactAttacker。所有13个预制体都有：OperatorUnit + OperatorBrain + UnitBlocker + OperatorAttackAnimator + BoxCollider2D(trigger) + Rigidbody2D(kinematic) + HealtherCanvas(UnitStatusUI)。
- [2026-08-10 19:29:49] [2026-08-10] 魔族干员精灵图尺寸统一方案：走路帧(720px/960px)和攻击帧(2048px)像素尺寸不同但原PPU均为100，导致切换动画时世界尺寸突变（走路7.2→攻击20.48）。修复：①按纹理尺寸设置不同PPU使所有帧世界尺寸统一为20.48（720px→PPU=35.16，960px→PPU=46.88，2048px→PPU=100不变）；星豹展示图1024px→PPU=50；熔岩虫目标10.24（720px→PPU=70.31）。②预制体scale统一：12个2048角色scale=0.0732，熔岩虫scale=0.1465，世界尺寸全部=1.50（与老干员1.43基本一致）。③BoxCollider2D统一为世界1.0×1.0。④HealtherCanvas按父级scale反向补偿（0.14/scale）。设计文档02-卡牌与角色设计.md中重装maxBlockCount已改为"无固定上限，按干员个体配置"。

### Reference
- [2026-07-30 18:36:18] Tuanjie engine .meta GUIDs are Base64, but Naninovel Assets.json expects 32-hex-char GUIDs. When creating new .nani scripts, the regenerated .meta Base64 GUID must be replaced with a hex GUID (e.g. System.Guid.NewGuid().ToString("N").ToLower()), and an entry with the hex GUID + "prefix": "Scripts" must be added to Assets/NaninovelData/Resources/Naninovel/Assets.json under the Scripts section. **Why:** previous AI created 奇遇测试 1.nani but didn't register it in Assets.json, and the auto-generated Base64 GUID was incompatible with hex format. **How to apply:** after creating a new .nani file, always: (1) ensure_has_meta, (2) replace Base64 guid with hex guid in .meta, (3) add {"guid": hexGuid, "prefix": "Scripts", "path": scriptName, "group": ""} to Assets.json.
- [2026-08-03 18:24:18] [2026-08-03] 干员玩法设计文档(03-干员玩法设计.md)最新版本要点：天赋卡=遗物（战后3选1），干员=角色，两者获取渠道完全分离。干员获取以剧情为主（ActConfig手动配置），奇遇/商店为辅。不引入"希望"货币，用阵容槽位(8)做约束。跨大局继承：通关后选5名干员带入下一大局，其余因剧情离队，等级保留、天赋卡清空。部署采用方案B（单实例，撤退返还50%DP+正常冷却，阵亡不返还+冷却×1.5）。
- [2026-08-09 18:54:41] E:\走路 目录存放各角色的走路帧拆分图片，文件夹名都是 ezgif 自动生成的（如 ezgif-46d9a8dc853962ab-png-split），无角色名标识。E:\走路\output\ 下有部分按角色名或编号整理的子文件夹（如 空之魔王、1、2、3）。E:\走路 根目录的帧图片为 960×960 RGB（无透明背景，需 rembg 抠图）；E:\走路\output\ 子文件夹（如 1/2/3）的帧图片为 720×720 RGBA（已有透明背景，可直接用）。walk_characters/1=死亡剑士(13帧)，walk_characters/2=火焰战士(13帧)，walk_characters/空之魔王=空之魔王(15帧)，ezgif-46d9a8dc853962ab-png-split=藤曼战士(15帧,960×960 RGB)，ezgif-24da9cdcfe261673-png-split=狂兽战士(15帧,960×960 RGB)，ezgif-2267c4976dd7f181-png-split=紫雷狼(15帧,960×960 RGB)，ezgif-6bde539de982e699-png-split=傀儡师(13帧,720×720 RGB)，output/3=时之魔王(13帧,720×720 RGBA已透明)，output/ezgif-6a8540243384a22c-png-split=死囚(13帧,720×720 RGBA已透明)，output/ezgif-2007b4b98c3777d1-png-split=魔物法师(15帧,960×960 RGBA已透明)，output/ezgif-6d1b8c2aa5d0f972-png-split=刀客(13帧,720×720 RGBA已透明)，output/ezgif-2c21654f092da8c4-png-split=蛮族女战士(15帧,960×960 RGBA已透明)，output/ezgif-1ca8a9b79eb2ff1f-png-split=打拳的小女孩(8帧,960×960 RGBA已透明)，output/ezgif-338fd03865ed9ed2-png-split=精灵弓箭手(15帧,960×960 RGBA已透明)，output/ezgif-611a1037f2a24bff-png-split=奶妈(13帧,720×720 RGBA已透明)，output/ezgif-3b4900a67b81a436-png-split=琴师(15帧,960×960 RGBA已透明)，output/ezgif-8fe877ff8e7b2b56-png-split=杀手(13帧,720×720 RGBA已透明)。女拳手走路帧在角色文件夹内(frame_0001~0025,319×392 RGBA已透明,由generate_sprite_sequence生成)。星豹走路帧在 Assets/new characters/魔族/星豹/走路帧/(16帧,960×960 RGBA已透明)。熔岩虫walk_frames+battle_frames各21帧(720×720 RGB→rembg)。雪女b_ezgif-frame-001~021为走路帧(720×720 RGB→rembg)。巨蟹走路帧在 Assets/TJGenerators/History/Sequence_20260807_212919/（25帧，已有透明背景）。**How to apply:** 需要找角色走路帧时，用 analyze_multimedia 逐个检查各文件夹的第一帧图片来识别角色身份。注意：部分角色在 人族/ 目录下而非 魔族/。圣龙骑士走路帧=output/ezgif-861a48b7dabd56e7-png-split(13帧,720×720)，output/ezgif-2e29f602ec26109a-png-split=猎魔人(15帧,960×960)，output/ezgif-83c8331a1bdddef8-png-split=卫兵(13帧,720×720)，output/ezgif-64e1162580958594-png-split=火炮手(13帧,720×720)，output/ezgif-67f30549be47dd10-png-split=时之勇者(13帧,720×720)。皇家护卫队队长走路帧在 walk_characters/新补文件夹(15帧,960×960 RGBA已透明)。






